# 项目审查与四天面试准备

审查日期：2026-09-11。代码基准：`3925539`，同时阅读了工作区尚未提交的 `KNOWN_ISSUES.md`。简历核验以用户本轮粘贴的五条项目描述为准。

## 1. 审查发现，按优先级排序

### [P1] 解析自检未通过仍能自动放行

位置：`application/agents/parser_agent.py:227`、`:322`；`application/agents/risk_control_agent.py:25`、`:246`；`application/orchestrators/order_processing.py:203`。

解析器二次抽取仍存在漏行、原文无法定位等问题时，只把行置信度降到约 0.7。下游把低置信度列为 medium，而最终只有 high 才阻止完成。原先“任何 issue 送审”改为“只有 high 送审”时，没有同步修改解析失败的传递契约。

本次无需调用模型的复现：原文两行，解析结果一行，重复抽取仍一行；解析器报告未解决，风控却返回 `needs_confirmation=false`、`overall_confidence=0.9`。这直接违背日志“交人工确认”，也削弱简历中的拒识保证。现有测试只确认置信度下降，没有验证真实风控后的终态。

必须修复：把未解决的抽取问题作为当前订单的结构化结果传递并持久化，明确阻止自动完成。模型自报低置信度和确定性校验失败应分别定义政策；不能只靠一个可丢失的浮点分数传信号。验收用真实 Parser 检查逻辑、Matching、Risk 和 Orchestrator 串联，只替换模型调用，断言终态及审核理由。

### [P1] 高相似度会掩盖关键规格缺失，错误 SKU 可以直接完成

位置：`application/agents/matching_agent.py:84`、`:118`、`:142`；`tests/test_matching_ambiguity.py:76`。

存在两条通道：规格在目录里唯一时，即使名称明确冲突，也以 1.0 分匹配；确定性匹配拒绝不完整规格后，向量分支仍直接选择 Top-1，没有检查关键属性、候选歧义或冲突。

本次微型复现：目录是“螺丝 / M8”，输入“完全不同的产品 / M8”，仍匹配螺丝 SKU，分数 1.0。现有歧义测试给向量分支返回空列表，只能证明“没有候选时不会匹配”，不能证明“有相似候选时不会误选”。

真实数据重放案例：`mfg_auto_approve_0260` 第三行，标注规格为 `CJX2-0910 AC220V 10`，旧解析结果漏掉末尾 `10`。当前匹配选择 `ELC-025`，其规格末尾为 `01`，得分约 0.98397；正确 SKU 是 `ELC-001`。价格异常仅 medium，这单仍自动放行。重放中共有 22 行“SKU 错误且分数至少 0.8”，其中并非每一行所在订单都自动放行。

必须修复：名称/登记别名兼容性、关键规格一致性、目录歧义共同决定是否接受候选。把召回候选与已确认 SKU 区分；缺少足以区分候选的属性时送审。候选分差可作辅助，但不能代替规格检查。不要仅提高 0.8 阈值，也不要为了匹配而自动补全会改变采购对象的属性。至少增加名称冲突、同名异规格、高分近邻、缺关键后缀四类负例。

### [P1] 数据库写入仍是全量覆盖，多个实例会丢订单

位置：`application/services/order_manager.py:143`、`:161`；`infrastructure/repositories/order_repository.py:75`、`:133`。

每个 Manager 启动时加载全部订单到内存，每次状态变化都把全部快照写回；仓储先 `DELETE FROM orders` 再写入本实例内存。两个实例各持旧快照，后写入者会删除另一个实例新增的订单；查询也读不到别的实例的新状态。

本次用相同仓储契约的内存替身复现：两个 Manager 初始化后分别创建 A、B，存储最终只剩 B。SQL 的全表删除路径经静态核验；本次没有对真实 PostgreSQL 做并发破坏性实验。默认 Docker 是一个同步 worker，降低了当前默认部署触发频率，但不能把 worker 数改大来解决吞吐而不先修这里。

必须修复：仓储按订单读写，状态更新使用事务及预期状态/版本条件，状态历史与结果在一致的提交中保存。确认/拒绝应竞争同一状态转换，失败者返回冲突。验收必须有两个独立 Manager、真实 PostgreSQL、交错创建、交错确认及重启读取的测试。仅把 DELETE 改成全量 UPSERT 仍会覆盖旧状态，并不够。

### [P1] 自建集旧基线被误用于解释当前代码

位置：`evaluation/results/baseline_generated_complex_test_20260910_234250/run_manifest.json:12`；`KNOWN_ISSUES.md:132`、`:142`。

240 条报告记录提交 `00f4656`。逐行预测没有 `business_decision`，风险只有旧版的 `price_abnormal`、`low_match_score`、`invalid_date_format`，没有当前新增的 high 规则。当前指标代码重新计算该报告与存档 summary 完全一致，所以算术没有问题；问题在版本归属和根因解释。

“93.75% 送审由当前任意 HIGH 规则导致”不成立：这份报告甚至没有 high issue。“当前 AUTO_CORRECT 分支不可达”也不成立：当前代码显式返回该动作，现有测试通过，本次重放更实际输出 79 次。

必须修复：保留旧报告作为旧基线，把已知问题的错误归因更正；固定代码、配置、评测时点，在新目录生成当前版本端到端报告。此次下游重放可用于定位收益，不能冒充新 Parser 的端到端结果。

### [P1] 评测中的“错误自动放行率”漏掉已放行订单的内容错误

位置：`evaluation/metrics.py:177`、`:185`、`:215`；`evaluation/run_evaluation.py:404`。

当前错误放行只检查“标注 manual_review，预测不是 manual_review”。若标注本来可以自动通过，但系统选错 SKU，仍计为没有错误放行。本次接触器案例正是这种情况。业务动作准确和交付内容正确是两个不同问题。

旧版报告的 1.67% 是 `4/240`。同一批数据，风险订单漏审率是 `4/80=5%`；自动放行订单中，仅按已知 manual 标签计算的不安全占比是 `4/15=26.67%`，若纳入内容错误还可能更高。三者分母不同，不能互换。

另外，失败样本传 `prediction=None`，被排除在字段和决策分母外；这是条件于成功的指标，不是全样本效果。额外生成的行会影响条数准确率，却不会进入字段准确率分母。无业务标注的 CORD 显示错误放行 0%，实际应为 N/A。

必须修复：同时报告执行成功率、全样本字段/订单正确性、仅成功样本条件指标、风险召回、自动处理覆盖率、已放行订单内容错误率。对未知标注显示 N/A。把缺失预测作为失败保留，但不能把技术失败擅自算成“已自动放行”。增加漏行、多行、失败、正确动作但错误 SKU 的固定评测用例。

### [P1] 并发评测实际共享同一个 Orchestrator

位置：`evaluation/run_evaluation.py:370`、`:378`。

`get_orchestrator()` 在调用 `pool.submit` 的主线程求值，而不是在工作线程求值，因此 `threading.local()` 没有实现预期的每线程隔离。所有任务共享同一 Parser、`last_self_correction`、Manager 及可变订单字典，可能出现诊断覆盖和快照迭代期间字典变化。不能据此断言已归档每行都错，但并发安全的设计意图确实未实现。

必须修复：在工作线程入口取得其本地 Orchestrator，保证可变订单上下文隔离。用屏障测试并发执行时的实例身份及订单归属，不能只跑几次“没报错”。

### [P1] 补跑覆盖原始尝试，没有验证运行身份一致

位置：`evaluation/run_evaluation.py:329`、`:386`、`:428`、`:436`。

补跑只按文档 stem 和 success 决定保留记录，不验证代码、提示词、数据、标注、配置是否一致，最后覆盖 manifest 和失败记录。新旧版本结果可被混成一份报告，旧成功行却挂到最新 code_version 下。Git HEAD 也不能反映脏工作区代码。

CORD 的 100/100 是补跑后的最终覆盖，不能当首次成功率；原 84/100、欠费失败和 JSON 输出失败应分别保留。JSON 失败是系统要处理的外部依赖可靠性问题，不能因重跑成功就称其“与流水线无关”。

必须修复：冻结输入/标注/代码或工作区哈希、模型与参数、提示词、索引、时点；同一运行恢复时验证这些身份，发生变化建立新运行。每次尝试追加记录，分别报告首次、重试后成功率及累计耗时/成本。中途逐样本落盘，避免进程退出丢掉一整轮。

### [P1] 干净启动没有初始化索引，健康检查仍报 healthy

位置：`infrastructure/vector_store/faiss_manager.py:64`、`:80`；`Dockerfile:19`；`interfaces/http/flask_app.py:41`；`.gitignore`。

索引文件被 Git 忽略，缺失时创建空索引；启动命令没有执行已有初始化脚本。本机已有数据目录的成功不能证明新机器的一键启动成功。空索引会使物料无法匹配，而 health 路由不检查数据库、模型或索引是否就绪。

必须修复：把已有标准物料 CSV 的索引初始化纳入明确的启动流程，加载后校验数量和版本，镜像内模型以离线方式加载；就绪检查能暴露缺索引/数据库不可用。用全新数据目录验证，不破坏现有目录。完整生成集也被忽略，仓库中未找到其生成脚本，评测复现需要附可获取的数据包或确定性的生成方法。

### [P2] “缺失字段留空并由下游处理”的契约不完整

位置：`domain/models.py:14`；`application/agents/parser_agent.py:263`；`application/agents/risk_control_agent.py:116`。

数量和物料名称仍为必填，数量缺失不能进入下游留空处理；单位、单价缺失虽然能解析，但可能直接完成，没有明确“不适用”和“缺失未核验”的区分。Parser 将缺规格统一当成重试问题，容易为原文真正缺失的字段多调用一次模型。金额检查还有一个分歧：Parser 在单价缺失时跳过，Risk 将缺失单价按零计算。

必须在面试前明确字段政策：哪些字段缺失必须拒识/送审，哪些允许缺失，金额无法核验如何表示。保留未知值，禁止猜数量；在 schema 与下游验证中落实同一约定。数值还应拒绝 NaN/Infinity，置信度应限制在 [0,1]。Pydantic 校验不等于字段真实，也不等于约束解码。

### [P2] 当天交期被判过去，失败状态缺乏中断收尾

位置：`application/agents/risk_control_agent.py:161`；`application/pipeline/stages.py:43`；`application/orchestrators/order_processing.py:97`。

正常运行把交期当天零点与当前 datetime 比较，当天即被判过去。本次复现为“2026-09-11 早于评估时点 2026-09-11”。日期语义应按日期比较，并注入同一评测时点。

外层异常捕获只返回错误；阶段异常抛出或 worker 被杀时，没有完整的重启收尾机制，订单可留在 parsing 等中间状态。现有“恢复测试”验证完成快照重新加载，不证明断点续跑。可先实现启动后对被中断订单明确标记失败及重提路径，不宣称已经具备自动恢复执行。

### [P2] 请求时间预算和现有延迟冲突

位置：`Dockerfile:19`、`frontend/nginx.conf:16`、`application/agents/parser_agent.py:50`。

Nginx 等待 10 分钟，Gunicorn 同步 worker 仍为 180 秒，模型客户端未配置明确的端到端预算。CORD 存档中 p50=184.32s、p95=310.23s，51/100 超过 180s。该数据来自本地并发评测，不是 Docker HTTP 压测，不能直接推算生产失败率，但足以说明必须通过实际部署验证。

四天内最低闭环：明确模型超时、重试次数、请求预算和失败状态，选定支持文档大小；用实际 HTTP 通道跑图片和长单。先修仓储再考虑多 worker。完整持久队列属于后续扩展，不能用简单延长前端等待时间来宣称解决恢复和吞吐。

## 2. 其他观察与范围边界

- PDF 只有文本层提取，没有扫描 PDF OCR；混合 PDF 会跳过无文本页。Excel 默认只读首个 sheet；`.xls` 在依赖清单中没有声明 pandas 通常需要的 xlrd。简历必须限定已验证格式边界，或补明确拒绝/支持测试。
- 自纠错第二次请求没有带第一次完整响应，“其余字段不变”没有实际可核对的基准。`last_self_correction` 没有持久化进订单，历史不能回答哪些字段被改、为什么改。金额不一致也可能是源单据事实，不能让模型为凑平账而改原始数值。
- `auto_correct` 存在，但判定只比较名称。返回对象保留原始规格/单位，没有完整的标准规格、标准单位和逐字段变更轨迹，不能宣称实现所有单位换算或规格修正。
- 人工审核有通过/拒绝和状态历史，但不能在界面修改 SKU/数量、重新验规则，没有审核人和备注。它是确认机制；不要说成完整人工纠错工作台。历史 risk 与当前审核结果最好分开保存，而非抹掉历史风险。
- 前端失败详情通常在 `data.message`，客户端只读顶层 message，实际会丢失具体错误；订单级风险索引 -1 显示成“第 0 项”；新增 business_decision 未在界面展示；规格/单位类型没有同步可空。
- 规则中包装数量一律 10、价格上限 1.5 倍属于模拟业务政策，需要明确来源。不要根据旧版误报率盲目降级。对真实采购应按物料单位/包装定义规则。
- 向量索引是 384 维 IndexFlatL2、精确检索，不是 HNSW/IVF。当前库 720 SKU，采用简单索引合理。分数 `clip(1-d/2,0,1)` 不是概率；单位向量前提下，FAISS 返回平方 L2 距离，该表达式对应截断后的余弦相似度。需确认模型输出归一化，阈值须用独立开发集校准。
- 模型自报解析置信度、检索相似度、规则扣分都不能解释成“正确概率”。当前 overall_confidence 是启发式，受问题条数影响，不是统计校准结果。
- 未运行浏览器视觉检查、真实 PostgreSQL 并发测试、容器重建或收费模型全量评测。无新代码修复已被宣称完成。

## 3. 验证结果及数据解释

### 已完成验证

- 后端 unittest：32 项通过。
- 前端 Vitest：4 项通过；TypeScript 与 Vite 构建通过。
- 自建集 240 条原始预测按当前指标重新汇总，与归档 summary 完全一致。
- 微型复现：未解决解析问题仍放行、名称冲突却满分匹配、双 Manager 覆盖丢单、当天日期误判。
- 用本机现存真实 FAISS 索引和嵌入模型离线重放，未调用收费 LLM。

### 三种结果必须分开

| 结果 | 样本与方法 | SKU 命中 | 动作命中 | 可以说明什么 |
| --- | --- | --- | --- | --- |
| 旧自建集基线 | 240 份 xlsx、旧版解析和下游 | 4736/5882，80.52% | 84/240，35.00% | 旧版问题和改动起点 |
| 本次下游重放 | 保留旧 ParsedOrder，运行当前 Matching/Risk/决策；评测日固定 2026-09-01 | 5805/5882，98.69% | 239/240，99.58% | 下游改动有明显收益，仍有高分 SKU 错配 |
| CORD 归档 | 100 份公开收据图片，补跑后汇总 | 无 SKU 标注，不适用 | 无动作标注，不适用 | 当前图像通路的域外抽取表现 |

本次重放为诊断实验，不经过新版 Parser、自纠错、HTTP、数据库。为节约重复计算，对 31 个需要向量召回的独特查询批量编码并缓存结果，再逐订单执行现有匹配与风控。运行固定时点来自生成集的 dataset_summary。不得把这个结果写成“新版端到端 99.58%”。

重放动作交叉表：

| 标注 | auto_approve | auto_correct | manual_review |
| --- | ---: | ---: | ---: |
| auto_approve，80 条 | 80 | 0 | 0 |
| auto_correct，80 条 | 0 | 79 | 1 |
| manual_review，80 条 | 0 | 0 | 80 |

重放的 80 条风险单中，high 类型按订单计数：金额不一致 41、价格超政策 28、未知/不可靠物料 43、非包装数量 26；可重叠。唯一 auto_correct 送审原因为不可靠物料。这个结果不支持“现在仍需全面降低风控严重级别”。

CORD 的字段分母与缺失标注区分有价值：名称 120/257=46.69%，数量 212/224=94.64%，单价 34/67=50.75%，总额 64/89=71.91%，条数完全相同 73/100。按行序做严格匹配，漏行/插行会连带影响后续行；需要抽样核查对齐与 OCR 错误，不能全部归因于模型名称抽取方差。该指标也不是官方 CORD benchmark 分数。

CORD 在语言、品类、票据布局上与制造订单不同，不能替代中文制造业的真实/独立模板测试。240 个自建测试样本全是 xlsx，不能据此宣称多格式鲁棒性；同一生成体系下的 train/dev/test 也不等于模板泛化已经验证。

## 4. 简历逐条核验

| 简历主张 | 判断 | 面试应如何限定 |
| --- | --- | --- |
| Multi-Agent 工作流与四类输入 | 主流程存在；术语偏宽 | 实际是固定编排的 LLM 文档工作流，Parser 调模型，Matching/Risk 是确定性组件；PDF 当前是文本型 |
| LCEL + Pydantic 强约束 ParsedOrder | 有该链，但“强约束”易引起误解 | 是提示格式、输出解析、类型校验及失败重试，不是约束生成，也不保证事实正确 |
| 缺失字段保留 null，下游判断 | 部分符合 | Optional 字段成立，quantity/name 不成立；缺失单价/单位的政策要补齐 |
| FAISS Top-K 与风险规则 | 基本符合 | 已新增确定性优先路径，向量 k=3 但没有 reranker；评分不是概率；规则是模拟业务政策 |
| 低分/风险不自动完成、人工确认及历史 | 部分符合，有关键回归 | 低匹配分会送审；不是所有风险都阻止完成，自检未解决漏洞必须修 |
| 分层评测与 Fake Agent 回归 | 符合，但结论需要补强 | 现有测试证明部分契约；恢复是快照重载，不能说进程崩溃后断点续跑；新版本仍需端到端评测 |

总体判断：这是有实际实现和评测基础的个人 Agent 工程项目，适合面试。当前不宜包装成成熟生产系统，也没有证据支持多个自主 Agent 协商规划。五条描述并非整体失实，但三处绝对化措辞需要主动限定：Multi-Agent 的含义、结构约束与事实正确的区别、风险拒识的覆盖范围。

可以说：“我把不确定的模型抽取与确定性的物料、业务规则分开，做了可追踪的状态流转和人工确认。它目前采用固定工作流；我的重点是用评测找出抽取、匹配和最终放行之间的错误传播，而不是让多个模型互相讨论。”

## 5. 四天内必须完成的最小闭环

这是按约 4 个专注工作日估计的安排，不是承诺完成时间。先修会导致错误交付的问题，保留一天用于冻结版本、验证和面试练习。

| 时间 | 工作 | 验收标准 |
| --- | --- | --- |
| 第一天 | 修自检失败传递、名称/规格歧义接受条件；明确缺失字段政策；修当天交期 | 四类高风险反例用真实下游跑通，错误订单不可自动完成，原因可查询 |
| 第二天上午 | 修并发评测、运行身份、失败/内容错误指标；更正 KNOWN_ISSUES | 同运行不混版本；首次与重试分别统计；正确动作错误 SKU 能被检出 |
| 第二天下午 | 用开发集校准，冻结后跑当前端到端结果 | 自建三类都有；至少补一组独立模板、PDF/图片、缺字段和同名异规格；保留所有失败 |
| 第三天 | 单订单数据库读写和条件状态转换；索引初始化、离线模型、超时与中断收尾 | 双 Manager 不丢单；确认/拒绝冲突可解释；干净目录可启动；实际 HTTP 失败可查 |
| 第四天 | 冻结版本，检查结果与简历，录制演示，口述与追问训练 | 能现场展示正常、名称归一、歧义、解析失败、人工确认、重启读取；每个指标说清版本/分母 |

不要为四天目标引入自主规划、多个 LLM 辩论、LangGraph 迁移、长期记忆或大规模任务队列。它们不能直接修复当前错误交付路径。不要把“多加数据、换更大模型、阈值放低”当第一优先级。

思考开关 A/B 有价值，但排在正确性之后：先统计一次请求的调用次数、各阶段耗时、tokens、重试原因；原文确实缺规格时不要无效重试。再在固定开发集对比思考开/关，观察内容正确率和尾延迟。已有单例的 -70% 耗时改善不能当整体结果。

若第三天仍有核心问题未闭环，停止扩功能，优先完成已选修复和证据。简历讲清单实例演示/原型的限制，不用未完成的设计承诺掩盖现状。

## 6. 严厉追问时的回答框架

统一使用“当前实现 -> 为什么这样设计 -> 我掌握的证据 -> 已知边界/下一步”顺序。先承认事实，再解释选择。下面都是对当前代码的描述；修复以后应更新为实际完成的行为。

### 你这不就是三个类，为什么叫 Multi-Agent？

“严格说是固定编排的 Agent 工作流。Parser 使用模型，Matching 做目录和向量检索，Risk 做业务校验，后两者没有自主规划。订单任务路径明确、错误代价较高，我选择可验证的流程。项目的价值是模型输出如何被约束、核验、拒识和追踪；我没有实现多智能体协商。”

追问“为什么不用一个大 prompt”：单 prompt 可以做基线，但主数据匹配、价格政策不应由模型自由生成；拆分可单独回放每阶段，定位损失来源。不要编造尚未跑过的单 prompt 对比结果。

### Pydantic 怎么防幻觉？

“它不能证明事实正确，只能校验可解析结构和声明的字段约束。即使格式正确，数量或规格仍可能错。我用原文定位、明细条数、金额一致性和目录匹配补充核验；当前这些检查仍不完备。发现无法解决的问题必须送审，不能把 schema 通过等同业务通过。”

追问“JSON mode 呢”：格式能力与事实核验分别讨论；当前代码没有使用 constrained decoding。不要把模型可支持的能力当成项目已实现。

### 0.98 分为什么还会错 SKU？

“这个分是相似度，不是正确率。相似型号仅一个属性不同，语义向量仍可能很近。我在真实解析输出的重放中找到了接触器 10/01 后缀问题。系统要先检查关键规格是否足够区分候选，不能让高向量分覆盖缺失证据。”

追问“Top-3 有什么意义”：当前只是召回后取最高分，没有重排；扩充 k 本身不会修复属性冲突。720 SKU 用精确索引足够，先保证接受条件和评测，再讨论大规模索引。

### 你的 99% 是不是刷出来的？

“旧版 240 份 xlsx 的 SKU 80.52%、动作 35%。我保留旧解析结果，重放当前下游后分别是 98.69%、99.58%，这仅证明下游改动收益，不是新版端到端指标。它仍有 22 行高分错 SKU，说明动作正确不代表订单正确。最终要看独立样本上已放行订单的内容正确性。”

追问“为什么 CORD 名称只有 46.69%”：公开收据是域外诊断，指标是按行序严格比较，不是官方 benchmark；名称、数量、金额分开报告，再抽查图像与对齐错误。不能说“通路 100% 成功所以准确”。

### 如何证明不把错误订单放过去？

“我不能证明绝对零错误。需要先定义放行条件，再同时看覆盖率、风险召回、已放行订单中字段和 SKU 错误率；有高分歧义等硬反例。有限样本里零错误也不代表线上零风险。”

追问阈值怎么定：开发集画接受覆盖率与错误率关系，结合误放行/误送审成本确定；测试集冻结后报告。当前 0.8 是默认阈值，未完成校准就不能说它最优。

### 接口等三分钟，服务挂了怎么办？

“当前是同步 HTTP，状态快照会保存，但不是持久化任务队列。进程中断后不会自动从检查点继续。必须先收敛调用预算并让中断订单可识别；要做线上长任务，再增加持久任务、幂等及阶段恢复，不能用线程池冒充可靠队列。”

### PostgreSQL 的事务为什么还会丢数据？

“事务保证一段写操作原子，并不保证写入的业务快照不是过期的。当前全量快照覆盖是设计缺陷；两个实例即使各自事务成功，后写仍能删掉前者的数据。解决要按订单读写，并对状态转换做并发条件校验。”

### 你在这个项目里最重要的贡献是什么？

用你实际完成并能逐行解释的改动回答：“从只看执行成功，转到看错误如何穿过解析、匹配和放行；建立可复查的评测与反例，让确定性规则承接模型的不确定输出。”不要把本报告建议和未完成修复说成已经实现。说明工具辅助开发的实际方式，重点证明你能理解、复现、取舍和负责验收。

## 7. 审查结论

值得投入四天。优先做实拒识、匹配安全、数据一致性和评测真实性，比继续堆 Agent 能力更能抵抗追问。本次证据同时说明改动确有价值，也说明漂亮的聚合指标仍可能隐藏错误交付。面试中最可信的材料是一个失败案例的完整因果链和经过验证的修复，而不是保证“绝不出错”。
