# 项目审查（2026-09-12）：修复核验与新增发现

审查日期：2026-09-12。代码基准：`163f9d2`（"fix system problem"），并阅读工作区尚未提交的改动。上一份审查见 [REVIEW_20260911.md](file:///Users/cmh/Documents/AGENT_project/REVIEW_20260911.md)。

说明本次基准已经漂移：上一份审查基于 `3925539`，此后经 `f12d259`（匹配/风控重构与重放回归）推进到 `163f9d2`。因此上一份的发现不能直接沿用，需要先核验处置情况，再谈新问题。

## 0. 本次审查的口径

- 工作区（未提交）：`M .gitignore`、`M FINAL_INTERVIEW_GUIDE.md`、`M application/agents/parser_agent.py`、`M infrastructure/document_processing/document_loader.py`、`D TODO_INTERVIEW_4_DAYS.md`、`D datasets/hybrid_manufacturing_categorical.csv`；未跟踪 `evaluation/results/excel_column_fix_20260912/http_evidence.json`、`scripts/verify_excel_http.py`、`tests/test_excel_parsing.py`。已跟踪文件净变化为 `125 insertions(+), 1365 deletions(-)`，即本轮的 Excel/PDF 修复尚未提交。
- 本次由我独立执行并给出原始输出的验证：后端 unittest（147 项，`OK`）、前端 Vitest（10 项，通过）、`git check-ignore`、以及对如下代码位置的逐行复核——[risk_control_agent.py](file:///Users/cmh/Documents/AGENT_project/application/agents/risk_control_agent.py#L306-L333)、[flask_app.py](file:///Users/cmh/Documents/AGENT_project/interfaces/http/flask_app.py#L47-L63)、[order_repository.py](file:///Users/cmh/Documents/AGENT_project/infrastructure/repositories/order_repository.py#L99-L116)、[matching_agent.py](file:///Users/cmh/Documents/AGENT_project/application/agents/matching_agent.py#L110-L196)、[container.py](file:///Users/cmh/Documents/AGENT_project/application/container.py#L18-L49)、[metrics.py](file:///Users/cmh/Documents/AGENT_project/evaluation/metrics.py#L263-L303)。
- 仅作静态阅读、未独立复跑的部分：真实 Qwen 端到端运行、PostgreSQL 并发破坏性实验、Docker 镜像重建、浏览器视觉检查。凡是引用这些结果处均标注来源目录，不当成本次实测。

## 1. 审查发现，按优先级排序

### 1.1 上一份审查问题的处置核验

#### [已修复] P1 解析自检未通过仍能自动放行

上一份指出：解析自检未解决时只把置信度降到 0.7，而 medium 不阻断完成，信号被丢弃。

现状核验：解析问题已作为结构化结果传递。[risk_control_agent.py](file:///Users/cmh/Documents/AGENT_project/application/agents/risk_control_agent.py#L72-L86) 的 `_check_parsing_issues` 对每个未解决问题产出 `UNRESOLVED_PARSING_PROBLEM` 且 `severity=HIGH`；同一文件 [:L306-L312](file:///Users/cmh/Documents/AGENT_project/application/agents/risk_control_agent.py#L306-L312) 在存在解析问题时直接返回 `needs_confirmation=True`、`overall_confidence=0.0`；[:L331-L333](file:///Users/cmh/Documents/AGENT_project/application/agents/risk_control_agent.py#L331-L333) 的终态判定为 `any(issue.severity == HIGH)`。上一份要求的两件事（结构化传递、与置信度解耦）均已落地，不再依赖一个可丢失的浮点分数。

剩余限制：自检本身仍是启发式，只能覆盖"行数不一致 / 原文无法定位"等可判定类型，不能声称能检测所有漏抽。

#### [已修复] P1 高相似度会掩盖关键规格缺失，错误 SKU 可直接完成

上一份指出：规格在目录唯一时即便名称冲突也打 1.0；向量兜底直接取 Top-1。

现状核验：目录键路径要求"名称或登记别名兼容 + 规格一致"且候选唯一，见 [matching_agent.py](file:///Users/cmh/Documents/AGENT_project/application/agents/matching_agent.py#L110-L127)。向量兜底改为显式接受条件 [:L150-L196](file:///Users/cmh/Documents/AGENT_project/application/agents/matching_agent.py#L150-L196)，依次要求：名称非空、规格非空、规格与最高候选**完全相等**（否则以 `_spec_conflict_reason` 报"缺少区分候选的关键属性"或"规格冲突"）、名称兼容、**无多候选歧义**（同一规格+名称命中多个 SKU 时拒绝而非任选）、分数达阈值。这正面回应了上一份"不要只提高 0.8 阈值"的要求——现在高向量分不能覆盖规格缺失。

剩余限制：接受条件是确定性的精确/前缀语义，对同义但非同串的规格写法仍可能落到送审，属于保守方向，可接受。

#### [已修复] P1 数据库写入仍是全量覆盖，多个实例会丢订单

上一份指出：仓储先 `DELETE FROM orders` 再全量回写，双实例互相删除。

现状核验：仓储抽象已改为按订单、带预期版本的条件写入——`update(snapshot, expected_updated_at)`、`delete(order_id, expected_updated_at)`、`archive(order_id, expected_updated_at)`，见 [order_repository.py](file:///Users/cmh/Documents/AGENT_project/infrastructure/repositories/order_repository.py#L16-L37)。实现使用单行 CAS：

```sql
UPDATE orders SET status = %s, updated_at = %s, snapshot = %s
WHERE order_id = %s AND snapshot->>'updated_at' = %s
```

并返回 `cursor.rowcount == 1`（[:L99-L116](file:///Users/cmh/Documents/AGENT_project/infrastructure/repositories/order_repository.py#L99-L116)）。`DELETE FROM orders` 已不存在于该文件。上一份要求的"预期状态/版本条件 + 失败者冲突"已实现。

剩余限制：这是**应用层乐观锁**，不是数据库级约束（没有 `SELECT ... FOR UPDATE` 或版本列唯一约束）；同一 `updated_at` 极端碰撞下的行为依赖时间戳单调递增保证。并发压测未做。

#### [已修复] P1 自建集旧基线被误用于解释当前代码

上一份指出：旧基线（`00f4656`）数字被当作当前代码结论，"当前 AUTO_CORRECT 分支不可达"是错误归因。

现状核验：[KNOWN_ISSUES.md](file:///Users/cmh/Documents/AGENT_project/KNOWN_ISSUES.md#L131-L176) 已更正版本归因，写明"旧版逻辑不得归因于当前 HIGH 规则"，撤销"不可达"判断，并给出"旧端到端基线 / 当前下游重放 / 待执行新端到端"三口径表。方向正确。

新问题：三口径表的"当前下游重放"一列数字与当前归档不符，详见 [P2 归档数字不一致](#p2-上一份审查引用的重放数字与当前归档不一致)。

#### [已修复] P1 "错误自动放行率"漏掉已放行订单的内容错误

上一份指出：只统计"标注 manual_review 却未送审"，漏掉动作正确但 SKU 选错的情况；失败样本被排除出分母；无标注集显示 0% 而非 N/A。

现状核验：[metrics.py](file:///Users/cmh/Documents/AGENT_project/evaluation/metrics.py#L263-L303) 现同时输出 `content_exact_match`、`content_exact_match_success_only`、`auto_handle_coverage`、`manual_review_recall`、`auto_release_content_error`，且 `format_counter` 在分母为 0 时输出 `N/A (0/0)`（[:L300-L309](file:///Users/cmh/Documents/AGENT_project/evaluation/metrics.py#L300-L309)）。summary 模板也明写"技术失败不算自动放行"、口径分离。上一份要求的分层指标已具备。

证据：`pdf_layout_fix` summary 出现 `Auto-release content-error rate: 0/3`、`Manual-review recall: 2/2`，即新口径已在真实运行中生效。

#### [已修复] P1 并发评测实际共享同一个 Orchestrator

上一份指出：`get_orchestrator()` 在提交线程求值，`threading.local()` 未实现隔离。

现状核验：`tests/test_evaluation_concurrency.py` 已存在并被纳入本次 147 项测试（全部通过）。上一份要求"在工作线程入口取得本地 Orchestrator + 屏障测试实例身份"。本次未逐行复核线程实现，**仅确认测试存在且通过**，不对实现细节背书。

#### [已修复] P1 补跑覆盖原始尝试，没有验证运行身份一致

上一份指出：`--resume-run` 只按 stem+success 保留，会混版本；进程退出丢整轮。

现状核验：`evaluation/run_evaluation.py` 新增 `build_run_identity`，冻结 code_version / workspace_state / prompt_fingerprint / model_config / artifacts / dataset_fingerprint / annotation_fingerprint / identity_hash；每次尝试追加写 `attempts.jsonl`（逐样本落盘）；`tests/test_run_archive.py` 覆盖，包含在本次通过的 147 项内。

证据：`interview_acceptance_cpu_regression_20260912_002119/` 同时含 `attempts.jsonl`、`run_manifest.json`、`restart_evidence.json`，符合"每尝试追加记录"的形态。

#### [已修复] P1 干净启动没有初始化索引，健康检查仍报 healthy

上一份指出：索引被 Git 忽略、缺失时建空索引、health 不检查就绪。

现状核验：[container.py](file:///Users/cmh/Documents/AGENT_project/application/container.py#L18-L49) 的 `initialize()` 在"索引为空且无 metadata"时用已有物料 CSV 调 `store.add_documents(documents)`，随后 `validate_catalog_index` 校验数量一致；`readiness()` 额外校验目录/向量数量、数据库可达、并把超时在途单置失败。[flask_app.py](file:///Users/cmh/Documents/AGENT_project/interfaces/http/flask_app.py#L47-L63) 的 `/api/ready` 在异常时返回 **503**，且 `before_request` 对上传/文本入口做同样的 503 前置拦截。上一份要求"就绪检查能暴露缺索引/数据库不可用"已实现。

证据：本次测试运行中可见 `validate_catalog_index` 抛 `Material catalog, FAISS vectors and metadata counts differ` 并被 readiness 捕获为失败——正是负例测试路径，说明该校验确实生效。

#### [已修复] P2 "缺失字段留空并由下游处理"的契约不完整

现状核验：[constants.py](file:///Users/cmh/Documents/AGENT_project/domain/constants.py#L40-L48) 现存在 `FIELD_MISSING_POLICY`：name=reject、spec/quantity/unit_price=escalate、unit/delivery_date/total_amount=allow；`tests/test_field_policy.py` 覆盖。上一份要求的"明确哪些字段拒识/送审/允许缺失"已落地。

#### [部分修复] P2 当天交期被判过去，失败状态缺乏中断收尾

现状核验：评测时点已可通过 `RISK_EVALUATION_AS_OF` 注入（`config.py` 的 `RiskConfig.evaluation_as_of`），归档运行固定为 `2026-09-01`，日期语义按日期比较。[container.py](file:///Users/cmh/Documents/AGENT_project/application/container.py#L36-L48) 在初始化与 readiness 时调用 `fail_stale_processing_orders()`，把超过 180 秒未更新的在途单置为失败。

未闭环：这是"惰性清理"而非独立定时器，刚中断的订单可能短暂仍显示处理中；`FINAL_INTERVIEW_GUIDE.md` 已如实写明这一点。上一份要求的"明确标记失败 + 重提路径"已部分满足，未声称断点续跑。

#### [部分修复] P2 请求时间预算和现有延迟冲突

现状核验：`ChatOpenAI` 已设 `timeout=60`、`max_retries=0`、`temperature=0`、`max_tokens=4096`；Gunicorn `timeout=180`；LLM 调用次数上限为 1 次（自纠错时才第 2 次）。

未闭环：SDK 60 秒不是整单硬截止；本轮小样本平均延迟已降到 4.78s / 11.97s，但那是并发 1、依赖预热的结果，不能推出线上 SLA。HTTP 状态码尚未精细区分业务失败与上游故障。

### 1.2 本次新发现

#### [P1] 冻结夹具实际仍被 Git 忽略，`.gitignore` 的反否定失效

位置：[.gitignore](file:///Users/cmh/Documents/AGENT_project/.gitignore#L50-L60)。

本次实测（`git check-ignore -v`）：

```
.gitignore:50:datasets/self_built   datasets/self_built/excel_regression_v1/orders/mfg_auto_approve_0005.xlsx
.gitignore:50:datasets/self_built   datasets/self_built/interview_v1/smoke/orders/smoke_01_standard.txt
```

`git ls-files datasets/self_built` 只返回 `datasets/self_built/.gitkeep`——即 `interview_v1`、`pdf_regression_v1`、`excel_regression_v1` 的全部输入与标签**当前都不在版本控制内**。

根因是 Git 的规则：父目录被排除后，无法再重新包含其中的文件。文件中 `datasets/self_built`（第 50 行）把整个目录排除，其后的 `!datasets/self_built/...` 否定规则全部失效；第 57 行新增的 `datasets/self_built/*` 进一步排除子项，第 58 行的 `!.../excel_regression_v1/` 同样因为父目录被排除而无效。注释"Frozen interview fixtures must travel with their content hashes"与 [FINAL_INTERVIEW_GUIDE.md](file:///Users/cmh/Documents/AGENT_project/FINAL_INTERVIEW_GUIDE.md#L78) 的"原始二进制输入已解除忽略，需随本次改动一起提交"与事实不符。

影响：评测可复现性依赖的固定输入不在仓库里；manifest 里的字节哈希无从核对；换机器或他人复现时无法拿到同一批夹具。上一份审查的 P1"评测复现需要附可获取的数据包或确定性的生成方法"并未真正闭环。

修复方向：不要排除 `datasets/self_built` 目录本身，改为逐层排除 + 逐层重新包含（父目录与其下各级目录都要被重新包含），并用 `git check-ignore -v` 对三个夹具目录逐个验收；或显式 `git add -f` 后确认 `git ls-files` 命中。

#### [P1] 240 条自建集的生成脚本仍然缺失

上一份审查已指出"完整生成集也被忽略，仓库中未找到其生成脚本"。本次复查：`datasets/generated_complex/` 目录存在于本地（被忽略），但全仓库检索 `generated_complex` 只命中 `.gitignore`、`HANDOFF.md`、`KNOWN_ISSUES.md`、`REVIEW_20260911.md` 四个文档，**没有任何生成脚本**。`scripts/` 下与本主题相关的只有 [build_evaluation_samples.py](file:///Users/cmh/Documents/AGENT_project/scripts/build_evaluation_samples.py)（生成 `interview_v1`）。

影响：240 条基线与下游重放都无法被第三方重建或校验；这是当前最大的一处复现缺口，面试被追问"这些数据哪来的、能复现吗"时无法给出确定答案。

#### [P2] 上一份审查引用的重放数字与当前归档不一致

逐份核对 `evaluation/results/` 下四份重放归档的 `replay_summary.json`：

| 归档 | SKU Top-1 | 业务动作 |
| --- | --- | --- |
| `downstream_replay_baseline_240_20260911_201535` | 5668/5882 = 96.36% | 154/240 = 64.17% |
| `downstream_replay_aliases_240_20260911_202117` | 5815/5882 = 98.86% | 223/240 = 92.92% |
| `downstream_replay_aliases_asof_240_20260911_202442` | 5815/5882 = 98.86% | 223/240 = 92.92% |
| `downstream_replay_generated_complex_20260911_202838` | 5815/5882 = 98.86% | 223/240 = 92.92% |

[REVIEW_20260911.md](file:///Users/cmh/Documents/AGENT_project/REVIEW_20260911.md#L138) 第 3 节引用的是"5805/5882，98.69%"与"239/240，99.58%"。这两组值在现存归档中**找不到对应运行**，且都高于当前实测值。

同时，四份归档给出两组差异明显的分母内结果（动作 64.17% vs 92.92%），文档没有说明它们分别对应哪次代码状态。这违反上一份自己提出的"同一运行不混版本、每个指标说清版本与分母"。

建议：确认 98.69%/99.58% 的来源运行；若已不存在，在 `KNOWN_ISSUES.md` 与后续材料中更正为归档实测值，并给四份重放各标注它对应的提交或变更。

#### [P2] Excel 修复未提交，与归档证据的 `code_version` 存在落差

[evaluation/results/excel_column_fix_20260912/http_evidence.json](file:///Users/cmh/Documents/AGENT_project/evaluation/results/excel_column_fix_20260912/http_evidence.json#L2-L3) 记录 `code_version = 163f9d2...`，但产生该证据的 Excel 修复（`parser_agent.py` +95 行、`document_loader.py` +16 行）**仍在工作区未提交**。也就是说 `163f9d2` 无法定位到实际运行的源码状态。

缓解因素：证据里同时记录 `source_fingerprint`，且有 `scripts/verify_excel_http.py` 可复跑。但 `run_identity` 的 `workspace_state` 若未把脏工作区纳入，就仍会出现"证据挂在其内容不同的提交上"的情况——正是上一份 P1 想解决的问题在 Excel 这一轮重新出现。

建议：把本轮 Excel 修复提交后再归档一次证据，或确认证据里的指纹已覆盖脏工作区状态。

#### [P2] 文档重复与断链

- "P2-4"同名异义：[KNOWN_ISSUES.md](file:///Users/cmh/Documents/AGENT_project/KNOWN_ISSUES.md#L207-L210) 的 P2-4 指"嵌入模型冷启动约 11s"；`HANDOFF.md` 的 P2-4 指"评测样本生成器未闭环"。两个不同问题共用编号。
- `TODO_INTERVIEW_4_DAYS.md` 本次被删除（-352 行），但 `HANDOFF.md` 仍引用它；同时 `FINAL_INTERVIEW_GUIDE.md` 已声明自己是唯一准备入口，`HANDOFF.md` / `REVIEW_20260911.md` 降级为历史证据但未标注。
- 建议：在 `HANDOFF.md` 顶部加一行"已被 `FINAL_INTERVIEW_GUIDE.md` 取代"，或直接删除，避免面试前自己看错文档。

#### [观察] 其他一致性问题

- 重复审核（对已终态订单再确认/拒绝）返回 **400**，语义上更接近 **409 Conflict**；`FINAL_INTERVIEW_GUIDE.md` 也承认"HTTP 状态码尚未精细区分"。
- 历史归档 [cord_v2_test_20260911_012942/summary.md](file:///Users/cmh/Documents/AGENT_project/evaluation/results/cord_v2_test_20260911_012942/summary.md) 仍是新口径之前的格式（无标注指标显示 0.00% 而非 N/A），未重算。保留旧归档是对的，但需在文档里注明它是旧口径。

## 2. 其他观察与范围边界

- **PDF 通路已改，但边界要讲清**：改为用 PyMuPDF 提取逐页表格单元格与表外文字，无文字页明确报"不支持扫描页"而非静默略过。行数只接受从 1 连续编号，不可靠时记为未知。已验证的是**文本型** PDF；扫描件、复杂多页版式、真实图片质量均未验证。
- **Excel 通路已改，边界要讲清**：首张工作表输出结构化 JSON 行与单元格，保留标题与空单元格。逐项自检要求"可识别表头 + 序号连续"；未知布局没有这项保障，不能宣称支持任意工作簿。仍只读首个 sheet。
- **PDF/Excel 修复的方向值得肯定**：没有靠降低风控阈值或补造字段来让指标变好看，而是"发现不一致→最多一次模型自纠错→未解决则阻断自动通过"。这是上一份审查最希望看到的那种修法。
- **格式边界不变**：`SUPPORTED_EVAL_EXTENSIONS` 仍只含 PDF / EXCEL / IMAGE，评测不直接吃 `.txt/.csv`。
- **向量索引性质不变**：384 维 `IndexFlatL2` 精确检索，不是 HNSW/IVF；`clip(1-d/2,0,1)` 在单位向量前提下近似截断余弦，**不是概率**；代码未显式保证嵌入单位归一化，因此不要无条件称之为余弦相似度。720 SKU 用精确索引是合理的。
- **自主性限定不变**：Parser 调外部模型，Matching/Risk 是确定性组件；不存在多智能体协商或自主规划。术语纪律继续按 [FINAL_INTERVIEW_GUIDE.md](file:///Users/cmh/Documents/AGENT_project/FINAL_INTERVIEW_GUIDE.md#L113-L115) 与上一份审查的措辞执行。
- **auto_correct 语义限定不变**：仅表示按目录把等价规格或登记别名归一化，保留原值/标准值/依据/行索引；不是补造缺字段、改数量价格或猜采购意图。
- **人工确认语义限定不变**：是"审核覆盖系统建议"，不自动补齐缺失价格、不消除原风险；没有在线字段编辑、审核员身份认证与权限体系。

## 3. 验证结果及数据解释

### 本次独立执行的验证

- 后端 unittest：**147 项，`OK`**（6.5s）。测试输出里的 `ConnectionError: database unavailable` 与 `ValueError: Material catalog, FAISS vectors and metadata counts differ` 是就绪检查的**负例路径**，不是失败。
- 前端 Vitest：**10 项通过**（2 个文件）。
- `git check-ignore -v`：确认三个夹具目录被忽略（见 [P1](#p1-冻结夹具实际仍被-git-忽略gitignore-的反否定失效)）。
- 逐行复核：风控 HIGH 判定与解析问题传递、`/api/ready` 503 与上传前置拦截、仓储单行 CAS、向量候选四条接受条件、容器索引初始化与就绪校验、新增指标与 N/A 呈现。

### 四种（含历史）结果必须分开，不得混称

| 结果 | 样本与方法 | 关键数字 | 性质 |
| --- | --- | --- | --- |
| 旧端到端基线 | `00f4656`，240 份 xlsx，真实 Qwen 全链路 | SKU 80.52%，动作 35.00%（字段回退口径） | 旧版端到端，仅历史对照 |
| 当前下游重放 | 当前 HEAD，固定旧 `ParsedOrder` 只重放 Matching/Risk/决策，**不调 Qwen**，240 条，评测时点 2026-09-01 | SKU 5815/5882 = 98.86%；动作 223/240 = 92.92%；风险召回 77/80 = 96.25%；自动处理覆盖 149/240 = 62.08%；整单内容命中 185/240 = 77.08%；错误放行 3/240 = 1.25%；放行内容错误 0/149 = 0% | 下游重放，**非端到端** |
| 小样本真实端到端（`interview_v1`） | 12 冒烟 + 20 验收，真实 Qwen，含 TXT/XLSX/文本 PDF/PNG，含 30 行长单 | 验收 `cpu_regression`：字段 51/51 全对、整单内容 20/20、自动处理 12/20、风险召回 8/8、放行内容错误 0/12，平均 4.78s，47044 tokens；冒烟 v2：内容 11/12、SKU 12/13 | 定向小样本验收，不能外推总体准确率 |
| PDF 修复后真实端到端（`pdf_regression_v1`） | 5 份文本型 PDF，真实 Qwen | 整单内容 4/5；名称/数量/单价 53/53；规格与 SKU 52/53；自动处理 3/5；风险召回 2/2；放行内容错误 0/3；平均 11.97s；15972 tokens | 两份用户实际失败原单的修复回归 |
| Excel 修复 HTTP 实证 | 2 份原单，真实 HTTP + PostgreSQL | 两单均 `http_status 200`；正常单 23 行自动通过、风险单 25 行因总额差 12 元送审；各 1 次模型调用（约 24.6s / 26.3s） | 定向修复回归，不是全量准确率 |
| CORD v2 test | 100 张公开收据图片 | 补跑后 100/100 执行成功；决策类指标按契约 N/A | 域外诊断，非官方 benchmark |

关于下游重放：口径纪律仍然成立——`98.86% / 92.92%` 是**不经过 Parser、不调 Qwen** 的重放结果，不能写成"新版端到端准确率"；`80.52% / 35.00%` 也不能再用来描述当前代码。新增的"整单内容命中 77.08%"比单独的 SKU 或动作指标更能说明现状：动作命中 92.92% 与内容命中 77.08% 之间的差距，正是"动作对但内容错"的空间。

关于小样本：四组运行都属于"小而定向的工程验收"。冒烟 v1 → v2 调整了提示词（别名拆分），v2 仍保留 1 例失败（`smoke_06_high_price` 把"接近传感器 LJ8A3-1-Z/BX"整体塞进名称、规格为空，最终安全送审但内容解析错误）。**保留失败并如实讲述，比抹掉重跑更有说服力**。

关于 PDF：`4/5 整单内容正确` 的失败项，是缺规格项原文为占位符"—"、标签为空并保留隐藏 SKU；系统保留占位符并拒识，所以规格/SKU 记不命中。这是"标签与系统行为不一致"的真实差异，未通过改标签消除。

## 4. 简历逐条核验（更新）

| 简历主张 | 判断 | 面试应如何限定 |
| --- | --- | --- |
| Multi-Agent 工作流与四类输入 | 主流程存在；术语仍偏宽 | 实际是固定编排的 LLM 文档工作流；Parser 调模型，Matching/Risk 是确定性组件；PDF 仅文本型、Excel 仅首 sheet |
| LCEL + Pydantic 强约束 ParsedOrder | 有该链，"强约束"仍易误解 | 现在是 `prompt \| llm` 后显式 `parser.parse`；是结构/类型校验与失败重试，不是约束生成，也不保证事实正确 |
| 缺失字段保留 null，下游判断 | 已补齐政策 | `FIELD_MISSING_POLICY` 明确 name 拒识、spec/quantity/unit_price 送审、unit/delivery_date/total_amount 允许缺失 |
| FAISS Top-K 与风险规则 | 符合，且已加确定性接受条件 | 确定性优先 + 向量兜底，k=3 无 reranker；分数不是概率；风险规则是模拟业务政策 |
| 低分/风险不自动完成、人工确认及历史 | 已修复关键回归 | 解析问题结构化传递并强制送审；向量候选需过规格/名称/歧义多重条件；仍不能证明"零错误放行" |
| 分层评测与 Fake Agent 回归 | 符合，口径已收紧 | 已同时报告覆盖率、风险召回、放行内容错误、N/A 处理；**但归档夹具未入库、240 条生成脚本缺失，复现性仍有缺口** |

总体判断：这是一份有实际实现、有真实模型验收、并且**修复经得起逐条核对**的个人 Agent 工程项目，适合面试。上一份审查提出的七条 P1，本次逐条核实**均有对应实现或测试落地**，这是本轮最大的进步。剩余风险集中在**可复现性**（夹具未入库、生成脚本缺失）与**文档口径一致性**（重放数字、重复编号），而非核心正确性。仍然不宜包装成成熟生产系统，也没有证据支持多个自主 Agent 协商规划。

推荐开场沿用并补充：

> 这是我搭建的制造业订单处理原型，用 LLM 抽取字段，用物料主数据和确定性规则做匹配、拒识与人工审核。我把不确定的模型输出与确定性的目录、业务规则分开，重点解决了关键规格高相似误匹配、解析问题没有传到下游、并发快照覆盖和评测口径问题。最近一轮是我自己上传真实文件发现的 PDF 行数误报和 Excel 规格数字串入数量，我用结构化读取和单元格自检修掉，并保留了修复前后的失败记录。它目前是固定工作流，有真实 HTTP 与数据库证据，但复现依赖的数据包还没入库，这是我知道的下一步。

## 5. 剩余最小闭环

上一份的"四天计划"大部分已在本日完成，剩余项按面试前的优先级排列：

| 优先级 | 工作 | 验收标准 |
| --- | --- | --- |
| 1 | 修 `.gitignore` 反否定规则，把三个夹具目录与 Excel 证据纳入版本控制 | `git ls-files datasets/self_built` 命中 orders/annotations；`git check-ignore -v` 不再命中夹具；`git status` 干净后可提交 |
| 2 | 提交本轮 Excel/PDF 修复与新增测试，再归档一次 `http_evidence` | 证据的 `code_version` 与实际源码一致；`workspace_state` 无未提交改动 |
| 3 | 给 240 条自建集补一个确定性生成脚本或明确的数据包 | 第三方能用脚本重建同哈希输入；否则在文档中声明"该集不可复现"并降级其结论 |
| 4 | 更正 `REVIEW_20260911` / `KNOWN_ISSUES` 的重放数字，给四份重放标注对应提交 | 每个指标都能指到唯一运行目录；不再出现找不到出处的百分比 |
| 5 | 清理文档：`HANDOFF.md` 标注被取代或删除；消解 "P2-4" 同名异义 | 面试前只需看 `FINAL_INTERVIEW_GUIDE.md` 一个入口 |
| 6 | 本人演练：不看文档讲出五个答案、三个案例，并现场演示正常单/归一化单/审核流程 | 能逐行指出代码位置，每个指标说清版本与分母 |

**不要做**：新增 Agent、换框架、扩到完整 CORD、引入任务队列、大规模思考开关 A/B。这些不能修复当前的复现缺口；当前最缺的不是功能，是"别人能不能拿到同一批数据重跑出同一个数"。

## 6. 严厉追问时的回答框架

统一顺序：**当前实现 → 为什么这样设计 → 我掌握的证据 → 已知边界/下一步**。下面的答案都已按当前代码更新，可直接使用。

### 你这不就是三个类，为什么叫 Multi-Agent？

"严格说是固定编排的 Agent 工作流。Parser 使用模型，Matching 做目录和向量检索，Risk 做业务校验，后两者没有自主规划。订单任务路径明确、错误代价高，所以我选择可验证的流程。项目的价值是模型输出如何被约束、核验、拒识和追踪；我没有实现多智能体协商。"

追问"为什么不用一个大 prompt"：单 prompt 可以做基线，但主数据匹配、价格政策不应由模型自由生成；分层后可固定 `ParsedOrder` 单独回放每阶段，定位损失来源。我没有做单 prompt 对照实验，不会声称已量化证明分层更优。

### Pydantic 怎么防幻觉？

"它不能证明事实正确，只能校验可解析结构和字段约束。格式正确仍可能漏行或写错规格。我补了三层：解析自检（行数、原文定位）、确定性匹配接受条件（名称兼容、规格完全一致、无候选歧义）、以及业务规则核验。而且现在解析问题不再只降一个浮点分数，而是结构化传下去直接阻断自动完成。当前这套检查仍不完备，发现无法解决就必须送审。"

追问"JSON mode 呢"：格式能力和事实核验分开讨论；当前用的是提示词 + `parser.parse`，不是 constrained decoding。

### 0.98 分为什么还会错 SKU？

"这个分是相似度，不是正确率。相似型号只差一个后缀，语义向量仍可能很近。现在高分数不能决定接受——向量候选必须同时过规格完全一致、名称/别名兼容、无多候选歧义、分数达阈值四条。缺一条就保留候选但不接受 SKU，转人工。"

追问"Top-3 有什么意义"：当前是召回后按规则筛选，没有 reranker；扩 k 本身不修复属性冲突。720 SKU 用精确索引足够。

### 你的 99% 是不是刷出来的？

"不能这么说。旧版 240 条端到端是 SKU 80.52%、动作 35.00%；那是旧代码，已经明确标注。我保留旧 `ParsedOrder` 重放当前下游，得到 SKU 98.86%、动作 92.92%，但这**不经过 Parser、不调模型**，所以它只证明下游改动的收益，不是新版端到端指标。更能说明问题的是整单内容命中只有 77.08%——动作对不代表订单对。而且这批数据的生成脚本目前不在仓库里，复现性是我承认的缺口。"

### 如何证明不把错误订单放过去？

"我不能证明绝对零错误。现在的做法是同时看覆盖率、风险召回、放行订单内容错误率，并且把技术失败从'自动放行'里分出来、无标注显示 N/A。小样本验收里自动处理 12/20、风险召回 8/8、放行内容错误 0/12；有限样本里零错误不代表线上零风险。"

追问阈值：0.8 是默认阈值，未完成校准就不能说最优；校准要在开发集上画覆盖率-错误率曲线。

### 接口等三分钟，服务挂了怎么办？

"当前是同步 HTTP、单个同步 worker、Gunicorn timeout 180 秒；模型 SDK 超时 60 秒、不重试。状态快照会按订单做条件更新保存，但没有持久化任务队列，进程中断不会自动从检查点继续。启动和 readiness 会把超过 180 秒未更新的在途单置为失败并提示重提——这是惰性清理，不是独立定时器，刚中断的单可能短暂仍显示处理中。"

### PostgreSQL 的事务为什么还会丢数据？

"事务只保证一段写操作原子，不保证写入的业务快照不是过期的。之前的全量快照覆盖是设计缺陷，两个实例各自事务成功但后写会删掉前者的数据。现在改成按订单读写，并用 `WHERE order_id = ... AND snapshot->>'updated_at' = ...` 做条件更新，`rowcount != 1` 即冲突。它是应用层乐观锁，不是数据库级约束，这一点我会如实说明。"

### 你在这个项目里最重要的贡献是什么？

"从只看执行成功，转到看错误如何穿过解析、匹配和放行。我把不确定的模型输出与确定性的目录、政策分开，建立了可复查的评测与反例。最近一轮是我自己上传真实文件暴露的 PDF 行数误报和 Excel 规格串入数量——这类问题不会出现在我之前所有的小样本里，我是靠结构化读取和逐单元格自检定位并修掉的，而且保留了修复前的失败记录。"

不要把本报告的建议和未完成项说成已实现；说明工具辅助开发的实际方式，重点证明能理解、复现、取舍和负责验收。

## 7. 审查结论

可以进入面试准备，剩余工作以"补复现性"为主，不是"补功能"。

本轮的核心变化是上一份审查的七条 P1 全部有了对应实现与测试，且修法方向正确：解析问题结构化传递并阻断自动完成、高向量分不再覆盖规格缺失、仓储改按订单条件写入、评测指标分层并区分 N/A、索引初始化与就绪检查落到 503。这些都能逐行指出并当场演示。

但两处必须处理，否则会在追问中失守：其一，**固定夹具实际仍被 Git 忽略、240 条生成脚本缺失**，导致"这些数据能复现吗"没有确定答案；其二，**归档指标与文档表述不一致**（上一份引用的 98.69% / 99.58% 在现存归档中找不到出处），直接触碰上一份自己立的"每个指标说清版本与分母"的规矩。

面试中最可信的材料仍然是一个失败案例的完整因果链：这次是"用户上传真实文件 → PDF 行数误报 / Excel 规格串入数量 → 结构化读取 + 单元格自检 → 保留失败、定向复验、如实限定范围"。它比任何漂亮的聚合指标都更能说明你能对自己的实现负责。
