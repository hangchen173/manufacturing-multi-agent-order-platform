# 面试前最终指引：当前验证范围与剩余问题

更新：2026-09-12。基于 develop 的 f12d259 与当前未提交改动。本文是唯一准备入口；旧 HANDOFF、TODO 和评审记录只作历史证据，不再逐份执行。

## 1. 现在离目标还有多远

**更正：此前“工程收尾完成，可以冻结”的判断撤回。** 用户实际上传暴露了复杂 PDF 行数误报和 Excel 规格数字串入数量的问题。下面原 20 条结果是历史小样本证据，不能代表当前所有格式可靠。优先修复已确认的输入处理缺陷，再谈整体冻结。

### 2026-09-12 PDF 修复与验证

- 测试缺口已核实：原冒烟 3 份 PDF、验收 5 份 PDF 全是单页单明细；30 行长单是 TXT。旧 240 条复杂订单基线全是 XLSX。CORD 是收据图片，不经过 PDF 文本行数检查。
- PDF 读取改用现有 PyMuPDF 的表格提取，保存逐页表格单元格与表外文字；不再把整张表打散成无列边界的文本。无文字页面明确报不支持扫描页，不悄悄略过。
- 行数只接受从 1 开始连续的序号；PDF 从表格序号列读取并跨页合并。不可靠时记为未知，不拿材质数字 316 当序号。真正漏行仍有固定响应反例验证纠错和送审。
- 自检失败通过结构化解析问题阻断，不再覆盖所有明细的模型自报置信度为 0.7。
- 141 项后端测试通过，含新增 PDF 回归；旧复杂集 240 份 PDF 的读取行数均匹配标签，**这项没有调用模型，只验证读取和数行**。
- 新固定回归输入：`datasets/self_built/pdf_regression_v1/`，保留两份用户实际失败的复杂 PDF 和原 3 份短 PDF，标签复制原件，没有改成当前预测。
- 真实 Qwen 运行：`evaluation/results/pdf_layout_fix_20260912_013425/`。5/5 动作命中、4/5 整单内容命中，53/53 名称、数量、单价命中，52/53 规格与 SKU 命中。不得宣称全部字段全对。
- 30 行正常单 `mfg_auto_approve_0011.pdf`：一次抽取，约 31.13 秒；自动通过，无风险项，30 个 SKU 命中，交期全部为原文 2026-09-25。20 行风险 PDF 正确送审，不再出现行数误报；其缺规格项原文为占位符“—”、标签为空并保留隐藏 SKU，系统保留占位符并拒识，所以仍记规格/SKU 不命中。未改标签消除差异。
- 实际服务已重载修复。HTTP 复验正常单 `718a2b71-8a5b-4220-8f29-01191a25a387` 自动通过，约 33.95 秒；风险单 `e1ccebc4-d462-4695-ae4d-417c5ba18519` 送审，约 20.98 秒。两单均一次模型调用，全部交期保留，无行数误报。证据在上述新结果目录 `http_evidence.json`，可运行 `scripts/verify_pdf_http.py --output <新证据路径>` 重验（会调用真实模型）。旧失败记录没有修改。
- 旧标签未覆盖两份复杂 PDF 的交期，故另在实际 HTTP 验证脚本中逐行断言原文统一交期；不能用汇总表的 3/3 交期指标代表全部 53 行。
- **仍未解决：Excel 的空格文本化导致规格尾部数字串列、复杂制造业图片的充分验证。** 本轮 PDF 修复不等于这两项已完成。

项目定位：个人搭建的制造业订单解析与审核原型。固定流程是文档读取 → LLM 抽取 → 主数据匹配 → 确定性风控 → 人工审核。它有工程价值，也有明确边界；不是生产订单平台，没有自主规划、多 Agent 协商。

以下是上一轮收尾的历史任务表，当前状态以上方更正为准。

| 原定任务 | 上一轮记录 | 证据与边界 |
| --- | --- | --- |
| 固定模型、关闭思考 | 已完成 | Qwen qwen3.7-plus，文本与图片均真实调用；SDK 请求体验证 enable_thinking=false |
| 已知错误放行保护 | 已完成 | 关键规格、名称冲突、全目录歧义、缺字段和未解决解析问题有回归；没有为提高自动化率统一降低风险等级 |
| 评测与存储可信 | 已完成 | 首请求前 manifest、输入/标注/源码哈希、恢复测试、未知用量；按订单写入与并发条件更新，真实 PostgreSQL 验证 |
| 小样本验收、实际演示 | 已完成 | 两轮 12 条冒烟、首次 20 条验收、CPU 部署修复后复用 20 条回归；4 个 HTTP 文件案例与审核、重启验证 |
| 面试解释材料 | 材料已完成，待本人演练 | 本文包含答案、案例、口径、启动步骤和下一模型训练指令 |

上一轮收尾记录为 137 项后端、8 项前端通过；PDF 修复后后端为 141 项通过，文件切换修复后前端为 10 项通过。确定性测试不调用付费模型，也不证明模型准确率。下表和旧 20 条结果均需结合上方更正阅读。

现在不做：新增 Agent、换框架、改 DeepSeek、扩大 240 条或完整 CORD、引入队列、大规模思考开关 A/B。只修亲自演示发现的阻断问题，不追求项目零缺陷。

## 2. 历史 20 条回归结果

PDF 修复前的运行：`evaluation/results/interview_acceptance_cpu_regression_20260912_002119/`。这是**复用验收集的修复回归**，不是新抽取的盲测集，也不是此次 PDF 修复后的全格式成绩。

| 指标 | 最终结果 | 正确解释 |
| --- | --- | --- |
| 执行成功 | 20/20 | 全部文档产生有效结果，技术失败 0 |
| 整单内容命中 | 20/20 | 已标注订单字段、明细字段、行数与 SKU 同时正确 |
| SKU 命中 | 51/51 | 包含 48 个非空金标 SKU 和 3 个应拒识的 null；不能说识别了 51 个有效 SKU |
| 业务动作命中 | 20/20 | 按事先声明的模拟政策和固定标签 |
| 自动处理覆盖 | 12/20，60% | 包括直接通过与确定性归一化后通过 |
| 应审核风险单 | 8/8 送审 | 错误放行 0，技术失败 0；不代表线上风险召回恒为 100% |
| 放行内容错误 | 0/12 | 分母是自动处理订单，不是全量订单 |
| 模型调用与用量 | 20 次，47,044 tokens | 此轮实际模型调用 20 次，用量未知 0；文档重试和模型调用次数不是同一概念 |
| 耗时 | 平均 4.78 秒，中位数 3.24 秒，P95 4.69 秒 | 20 个样本，P95 用 nearest-rank；含 30 行长单，均值可能高于 P95；不是线上 SLA |

耗时为串行评测中每文档链路耗时，累计约 95.59 秒，依赖已预热，不包括首次启动。不能与旧模型单条测量拼接得出“全量提速 70%”。

**失败必须保留并会讲：**第二轮冒烟 `interview_smoke_nothink_v2_20260912_001545` 是 12/12 执行成功、12/12 动作正确，但只有 11/12 整单内容正确、12/13 SKU 命中。`smoke_06_high_price` 把“接近传感器 LJ8A3-1-Z/BX”整体放进名称，规格为空，最终安全送审但内容解析错误。动作正确不能掩盖抽取失败。第一轮冒烟也保留，发现别名拆分问题后调整提示词；不能将它删除后宣称一次成功。

首次 20 条验收在 `interview_acceptance_nothink_20260912_001655`，同样内容和动作 20/20，其中一张图片发生结构纠错；随后实际 HTTP 暴露部署崩溃，修复 CPU/线程配置后复用这 20 条回归。两次不能合并声称 40 条独立测试。

### 数据说明

- 固定集在 `datasets/self_built/interview_v1/`：12 条开发冒烟、20 条验收，含 TXT、XLSX、文本型 PDF、PNG，包含 30 行订单。
- 输入与标签来自目录和显式模拟政策，在模型运行前确定，不是系统预测当标签。模板不同不代表独立客户数据，同一生成体系仍可能有偏差。
- 字段按行序对齐，漏行参与判错，额外行影响行数和整单正确性。无标注字段没有被证明正确。
- 这是小而定向的工程验收，不足以估计真实制造业总体效果。没有客户生产数据，没有单 prompt 对照收益，也没有 FAISS 独立效果增益的证明。
- 原始二进制输入已解除忽略，需随本次改动一起提交；manifest 核对字节哈希。生成器不覆盖冻结版本，重新生成 PDF/XLSX 可能改变元数据，不能用新文件冒充旧运行输入。
- 旧 240 条重放的 223/240 动作命中、77/80 风险送审只属于旧 ParsedOrder 与旧标签。3 条差异已核到来源：被标“规格冲突”的字段实际与目录一致，另有副/kg/支的包装数量；当前模拟政策仅约束个/只/件为 10 的倍数。保留旧结果，不改标签刷分，不宣称新模型已有旧 240 条成绩。

## 3. 实现到底做到了什么

### 解析、匹配、风控

模型固定为 DashScope OpenAI-compatible `qwen3.7-plus`。temperature=0、max_tokens=4096、关闭思考、SDK timeout=60 秒、SDK retries=0；Parser 最多两次抽取。结构错误或可纠正的自检问题才触发第二次，带上前次输出并记录字段变化；网络超时直接失败，缺规格不靠重试补造，金额矛盾交风控判断。

Pydantic 是结构与类型校验，不是 constrained decoding，也不能保证字段来自原文。目前是 `prompt | llm` 后显式 `parser.parse`，不要声称代码仍恰好是三个对象连接的一条 LCEL 管道。缺失数量/价格保留 null，不能当零。

名称、规格、别名、关键属性与目录歧义决定能否接受 SKU。全目录已确认歧义，不能因为 Top-K 只返回一个候选而放行。FAISS 使用 IndexFlatL2，返回平方 L2，映射分不是正确概率。代码未显式保证嵌入单位归一化，因此不能把 1-d/2 无条件叫余弦相似度。720 SKU 用简单精确索引合理，目录接受路径和向量候选各有职责。

auto_correct 只表示按目录将等价规格或登记别名归一化，保留原值、标准值、依据和行索引；不是自动补造缺字段、改数量价格或猜采购意图。任何未解决的阻断风险仍转人工。

人工确认是**审核覆盖系统建议**，不会自动补齐缺失价格、消除原风险或重算成无风险单。结果保留原系统判定和审核记录。当前没有完整的在线字段编辑、审核员身份认证与权限体系，不能宣称生产级审批。扫描 PDF、多页复杂表格和各种真实图片质量均未全面验证；本轮 PDF 验收是文本型 PDF。

### 存储、异常、部署

去掉全表删除/全量快照回写和长期订单缓存。每次按订单读取，通过精确 updated_at 快照做条件更新；Manager 保证新版本时间单调增加。最终结果与状态、审核动作与状态历史一起保存；并发确认/拒绝只能一方成功。

真实 PostgreSQL 测试覆盖两个 Manager 交错创建更新、旧版本写入拒绝、并发审核一方获胜、终态不被迟到结果覆盖。这证明所测冲突场景，不能替代吞吐压测。

服务分存活 /api/health 和就绪 /api/ready，检查数据库、目录和真实向量索引；空索引可从目录初始化，损坏索引不假装就绪。启动预热嵌入模型。Mac 实际 Gunicorn 曾因 MPS 与 OpenMP 崩溃，最终固定 CPU 和 Torch/FAISS 各单线程，完成真实 HTTP 与 CPU 模型回归。

当前单个同步 Gunicorn worker，timeout=180 秒。SDK 60 秒不是整单硬截止；没有持久队列、自动断点续跑或服务总时限证明。启动、readiness 或后续入口会将超过 180 秒未更新的在途单置为失败并提示重提；这不是独立定时清理器，刚中断的单可能暂时仍显示处理中。

SDK 超时通过真实 SDK 的模拟 HTTP transport 注入测试：只调用一次，接口按现有契约返回 HTTP 400 / success=false，订单可查为 failed，诊断记录 APITimeoutError、用量未知。这不是付费接口真实等待 60 秒的试验；HTTP 状态码尚未精细区分所有业务与上游故障。

4 份真实文件经过本地 Gunicorn：正常、等价规格、规格歧义、缺单价。确认和拒绝成功，重复审核返回 400，每单仅一条审核记录。Gunicorn 替换 worker 后 4 份快照完全一致；此前真实崩溃的在途单转 failed。证据在最终结果目录的 http_evidence.json、restart_evidence.json。

前端实际检查正常、归一化与审核详情，桌面和手机可用，明细表在窄屏横向滚动；390px 屏幕下页面宽度与内容宽度均为 390px。额外通过页面调用真实模型提交两份缺单价文本，分别点击确认和拒绝，均显示对应终态和一条审核记录（历史 ID 前缀 97d61b8b、b38c6ead）。这两次是交互验证，不计入 20 条评测。Docker 配置已调整，但**本轮没有重建验证最终 Docker 镜像**；演示使用本地 Gunicorn、Vite 和一次性 PostgreSQL，8080 上旧容器不是当前成果。

## 4. 面试时五个回答

### “这也能叫 Multi-Agent？”

“严格说是固定编排的 LLM 业务工作流。Parser 调模型，Matching 和 Risk 主要是确定性组件，没有自主规划或多模型协商。订单处理路径明确，所以我优先让主数据、拒识和业务判断能独立验证。标题里的 Agent 指职责组件，我不会把它讲成自主智能体系统。”

### “一个 prompt 不就够了，你的架构有什么意义？”

“SKU、参考价和政策是外部事实，我希望它们由目录和规则决定。分层后可以固定 ParsedOrder 重放匹配和风控，分清抽取错、匹配错还是政策不一致，也能阻断错误传播。我没有做单 prompt 对照实验，因此不会声称量化证明分层优于所有单模型方案。”

### “Pydantic 都过了，为什么还会错？分数 0.99 为什么还拒绝？”

“结构正确不代表事实正确。模型可以输出合法 JSON 却漏一行，向量近邻也可能缺少关键后缀。分数只用于候选参考，缺关键属性和目录歧义不能被高分覆盖。冒烟里名称规格拆错，最后送审了，动作正确但我仍将内容记错。”

### “20 条全对有什么说服力？”

“只说明这些预先标注的定向场景跑通，不是总体准确率。数据是按目录和模拟政策自建，和真实客户分布有距离。我保留冒烟失败，区分首次验收与复用回归，同时报告 12/20 自动处理、0/12 放行内容错误、8/8 风险送审；下一步需要独立业务样本与错误分层，而不是继续在这 20 条上调分。”

### “两个人同时审核，或者模型挂了怎么办？”

“按订单快照版本做条件更新，审核状态和记录原子保存，冲突一方失败，真实数据库测试覆盖过。模型超时记失败并可查诊断；进程崩溃的旧在途订单后续会转失败，需要重新提交。目前同步单 worker，没有持久任务队列，不能声称高吞吐或自动恢复执行。”

### 三个案例，每个练到两分钟以内

1. **漏行**：连续抽取仍漏行 → 只降置信度可能不能阻断 → 结构化解析问题向下传递并送审 → 固定错误模型响应跑完整下游回归。限制：自检是启发式，不能检测所有漏抽。
2. **接触器/球阀规格歧义**：10/01 后缀或缩径属性缺失 → 向量高分不能确定采购对象 → 检查关键属性和全目录歧义，保留候选但不接受 SKU → 高分反例与正常对照一起测试。
3. **持久化**：两个实例读到旧快照 → 全量回写覆盖另一实例订单 → 按订单更新加版本条件，最终结果与审核原子落库 → PostgreSQL 交错写入、并发审核、worker 重启验证。限制：没有生产负载测试。

每个都按“输入、错误、根因、改法、验证、限制”讲。使用 AI 辅助开发如实说明；重点是能定位实现并解释取舍，不必声称从未参考任何工具或资料。

### 简历对齐结论

工作流、抽取、FAISS、确定性风险、拒识、人工审核、状态历史、分层评测和 Fake 回归有实现支撑。收紧两处用词：Multi-Agent 解释为固定工作流职责组件；“强约束输出”解释为提示词与结构校验，不是保证正确的强制解码。不要声称生产落地、已量化检索收益或全面高准确率。

推荐开场：“这是我搭建的制造业订单处理原型，用 LLM 抽取字段，用物料主数据和确定性规则做匹配、拒识与人工审核。我重点解决了关键规格高相似误匹配、解析问题没有传到下游、并发快照覆盖和评测口径问题，并用固定反例、真实数据库与小样本模型验收验证。它目前是固定工作流，支持演示和故障记录，还不是高吞吐生产系统。”

## 5. 本人只剩三件事

- [ ] 打开当前演示，亲手查看正常单、归一化单、拒识单，并重新提交一单完成审核。确认通过是人工覆盖，不是系统证明数据完整。
- [ ] 不看本文讲出五个答案、三个案例，能在代码里找到对应实现。
- [ ] 用下面的 prompt 做一次追问演练；只修理解错误或演示阻断，不加新功能。

当前演示：<http://127.0.0.1:5173/>；后端 <http://127.0.0.1:5001/api/ready>。历史 ID 前缀：fefbd61f 正常、b19e32c1 归一化、f9f77125 拒绝、2bb3fd15 人工通过。

服务关闭后，在项目根目录启动（依赖已安装、.env 有有效 Qwen key、缓存嵌入模型存在；下面密码仅为本地一次性测试库）：

```sh
docker start agent-interview-test-pg
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 DATABASE_URL=postgresql://postgres:interview-test-only@127.0.0.1:55433/interview_test DATA_DIR=output/interview_validation/data ORDER_AUTO_ARCHIVE_DAYS=0 .venv/bin/gunicorn -c gunicorn.conf.py --bind 127.0.0.1:5001 app:app
```

另一终端运行 `npm --prefix frontend run dev -- --host 127.0.0.1 --port 5173`。服务已在跑则不重复启动。若测试库容器已被删除，应新建 PostgreSQL 并配置 DATABASE_URL，旧历史记录不会凭空恢复。固定示例交期 2026-09-30，过期后实时演示会按实际日期送审；评测固定时点为 2026-09-01。

不付费复核：

```sh
TEST_DATABASE_URL=postgresql://postgres:interview-test-only@127.0.0.1:55433/interview_test RUN_EMBEDDING_INTEGRATION=1 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 .venv/bin/python -m unittest discover -s tests -q
npm --prefix frontend test
npm --prefix frontend run build
.venv/bin/python scripts/inspect_interview_fixtures.py
```

**现在没有必须追加的付费评测。** 改模型、提示词、抽取行为或匹配接受条件才需重新调用模型验收。换 DeepSeek 还需核实具体 model ID、图片能力、思考关闭参数、usage 和截断行为，新建运行不能 resume Qwen 结果。纯指标改动重算保留预测；纯存储/页面改动跑对应测试。

确需重跑时，用新运行名，约 20 次初始请求，纠错另计：

```sh
RISK_EVALUATION_AS_OF=2026-09-01 HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 .venv/bin/python evaluation/run_evaluation.py --input-dir datasets/self_built/interview_v1/acceptance/orders --annotation-dir datasets/self_built/interview_v1/acceptance/annotations --dataset-name interview_new_regression --concurrency 1
```

运行 manifest 应显示固定时点 2026-09-01。输入、模型、提示词和索引身份均应记录，不能拼接不同版本成绩。

## 给下一个模型的唯一训练 prompt

> 请阅读当前工作区 /Users/cmh/Documents/AGENT_project/FINAL_INTERVIEW_GUIDE.md，结合实际代码和最终验收产物，对我做字节 Agent 岗模拟面试。不要修改代码或新增计划文件。先核实实现，不能将文档勾选当作证明。每次只问一个问题，等我回答后指出不准确、回避或缺证据之处，给出能理解和复述的诚实答案，再追问一层。重点问：固定工作流是否算 Multi-Agent，为什么分层而非单 prompt，Pydantic 与事实正确性的边界，FAISS 平方 L2 和分数含义，关键规格拒识，缺字段和纠错，auto_correct 与人工覆盖的区别，数据生成偏差及评测分母，动作正确但内容错误，源码身份和恢复，PostgreSQL 条件更新，超时与崩溃处理。要求我用漏行、规格歧义和并发覆盖三个案例讲清输入、根因、改法、验证和限制。不要让我背“100%准确率”，应让我解释 20 条回归、12/20 自动处理、0/12 放行内容错误、8/8 风险送审及冒烟 11/12 内容正确的局限。最后让我演示一个正常单、一个归一化单和一个审核流程。目标是我能对自己的实现负责；发现不足先解释是否属于面试前必须修的阻断，避免重新扩展待办。
