# 面试前四天任务清单（按优先级重排）

制定日期：2026-09-11。依据：当前代码审查与 REVIEW_20260911.md。

本清单是待执行计划，不代表修复或新评测已经完成。当前不调用 Qwen、不修改业务代码。

## 排序原则

1. 按四层优先级排列：**第一层修错误交付，第二层修评测可信度，第三层修工程击穿点，第四层性能优化与扩展**。
2. 第一层"修完即验"：每个修复都绑定一段确定性回归（固定响应/本地索引），先证明危险反例被拦住，再进入第二层。
3. 第二层把"评测实例隔离"作为入口前置——**先修 harness，再信指标**；旧基线、新版下游重放、新版端到端三个口径必须分开。
4. **第四层任何项（思考 A/B、模型预热、参考价字典、更多公开数据集、更复杂编排）不得成为第一至第三层的前置。** 第四层可整体延期，不影响前三层交付。
5. 第三层是"能被面试官一问击穿"的工程缺陷，必须在冻结版本之前完成，但不占用第一、二层的执行槽位。

## 标记与执行约定

- **Qwen：不需要**：代码修改、确定性测试、已有预测重放即可完成。
- **Qwen：需要**：必须发送真实文本或图像请求，用于验证当前模型链路。
- **Qwen：部分需要**：实现及主要测试离线完成，最终端到端验收需要真实模型。
- 先用 Fake 模型输出复现和修复，再使用真实 Qwen 验收，避免边改边全量重跑。
- 自建数据开发集用于调试、选参数；冻结后使用测试集。已经反复分析的旧测试集应标为回归集，不能声称完全未见。
- 真实评测记录代码/工作区版本、提示词、模型及参数、输入和标注哈希、物料/索引版本、评测时点、尝试次数、tokens 与耗时。不得记录 API Key。
- 失败和重试必须保留；不同版本不能通过 resume 混成一份结果。

---

## 第一优先级：修复错误交付

目标：任何"自检未解决、规格歧义、关键字段缺失"的订单都不能错误自动完成。每一项都以确定性回归收尾。

### P1-1 修复 Parser 自检未解决仍自动完成

- [x] 将未解决的漏行、原文定位失败等问题作为当前订单的**结构化校验结果**传给下游，并持久化。
- [x] 明确校验失败的阻断条件：未解决问题作为阻断项进入风控终态，不仅依赖模型自报置信度或降分。
- [x] 用固定模型响应模拟连续两次漏行，串联真实检查、匹配、风控和状态机，断言终态为人工确认。
- [x] `parser_agent._parse_with_self_correction` 的未解决问题从内存字典提升为随订单持久化的字段。

**Qwen：部分需要。** 修复和确定性回归不需要；最终模型链路验收需要，可并入 P2-6 的冒烟集。

验收：未解决的确定性校验问题不能进入 completed；详情和重载后的快照均保留具体原因。真实模型没有触发指定错误时，不代替固定错误响应的回归证据。

### P1-2 修复名称冲突和规格歧义的接受条件

- [x] 删除"规格唯一便无视名称接受"的路径；接受条件检查标准名称/已登记别名兼容性。
- [x] 完整名称与规格的目录键如对应多个 SKU，显式标记歧义，不能默认取第一条。
- [x] FAISS 召回后检查关键规格，缺少区分候选的属性时保留候选并送审，不用高相似度覆盖硬冲突。
- [x] 区分召回候选和已接受 SKU，记录匹配依据和拒识原因。
- [x] 增加负例：名称冲突、同名异规格、高分相似候选、缺关键后缀。

**Qwen：不需要。** 使用保存的 ParsedOrder、真实本地 FAISS 索引或固定候选验证；新 Parser 对规格的抽取能力由 P2-6/P2-7 验证。

验收：关键规格不足或冲突时不自动选定 SKU；高相似度不覆盖硬冲突；标准完整规格与合法别名的正例仍能通过。

### P1-3 接触器 10/01 案例固定为回归测试

- [x] 将 `ELC-001`（CJX2-0910 AC220V 10）与 `ELC-025`（CJX2-0910 AC220V 01）同名异后缀案例固定为回归用例。
- [x] 断言后缀不同即视为不同 SKU，不得因名称高度一致而误配。
- [x] 不能靠提高相似度阈值掩盖问题；回归需覆盖确定性匹配与向量召回两条分支。

**Qwen：不需要。** 使用本地物料表与固定 ParsedOrder。

验收：10/01 后缀差异被稳定识别为不同 SKU；该用例进入常规测试集，防止回退。

### P1-4 明确缺失字段与业务规则政策

- [x] 列出名称、规格、数量、单位、单价、交期、总额的缺失处理：允许缺失、待核验或必须拒识/送审。
- [x] 对抽取 schema 和下游验证落实同一政策；禁止模型猜数量，未知字段保留未知。
- [x] 区分确定性阻断项与提示项；暂不引入综合风险评分。
- [x] 包装数量约束明确适用 SKU/单位或模拟业务范围，不能默认代表所有制造业物料。
- [x] 金额核验条件不足时明确标识，不把缺失单价按零计算以制造不一致。
- [x] 日期按日期语义比较，修复当日交期被判过去；评测使用固定时点。
- [x] 校验数值有限性和置信度范围。

**Qwen：不需要。** schema 和规则使用构造输入验证；真实缺字段抽取样本并入 P2-6/P2-7。

验收：每项政策有正常、缺失、非法输入用例；没有静默补造；不能仅为了降低送审率把问题统一降为 medium。

### P1-5 收敛 auto_correct 的实际输出

- [x] 限定为不改变采购意图的确定性归一化，例如登记别名与等价书写。
- [x] 保留原始值、标准值、变更字段和依据；仅名称比较不能代表所有字段归一化。
- [x] 不把猜测规格、调整数量、修改价格算作自动纠正；单位换算如无明确依据则不自动执行。

**Qwen：不需要。**

验收：auto_correct 必须有实际、可解释的归一化记录；无变更返回 auto_approve；存在阻断问题返回 manual_review。

---

## 第二优先级：修复评测可信度

目标：指标能支撑面试追问——样本、分子、分母、版本、是否经 Qwen 全部可追溯。**先完成 P2-0 隔离，再运行任何并发评测。**

### P2-0 修复并发评测实例隔离（入口前置）

- [x] 在工作线程内部获取线程本地 Orchestrator，避免在主线程求值后共享实例。
- [x] 隔离 Parser 诊断和订单上下文，明确共享 FAISS 的只读使用边界。
- [x] 用并发屏障验证实例身份、订单归属和结果隔离。

**Qwen：不需要。** 使用固定响应与屏障即可验证并发行为。

验收：并发任务不覆盖自纠错记录、不串订单，串行和并发的确定性结果一致。**此项未完成前，任何并发评测结果不计入指标。**

### P2-1 修复运行归档与补跑

- [x] 生成并校验运行身份，包含代码/工作区、提示词、模型配置、数据/标注、索引与评测时点。
- [x] 逐样本、逐尝试保存结果，补跑追加尝试记录，保留首次失败。
- [x] 同一运行身份不一致时拒绝 resume，建立新运行。
- [x] 分开汇总首次成功率、重试后成功率及累计耗时/tokens。

**Qwen：不需要。** 通过模拟中断、版本变化和固定响应验证。

验收：中断后可恢复已落盘结果；不同版本不会混算；报告能追溯每条最终预测来自哪一次尝试。

### P2-2 增加内容错误与全样本指标

- [x] 保留分层字段、SKU、动作指标，增加整单内容正确率。
- [x] 统计自动处理覆盖率、风险召回率、自动放行订单内容错误率。
- [x] 正常标签订单若被抽错关键字段或选错 SKU，必须计入已放行内容错误。
- [x] 同时报告全样本指标和成功样本条件指标；技术失败不等于自动放行。
- [x] 对额外行、漏行和行序对齐说明计分规则；无标注指标显示 N/A。
- [x] 增加失败、额外行、漏行、正确动作错误 SKU 的指标回归测试。

**Qwen：不需要。** 使用保存的预测和人工构造例子验证。

验收：接触器错误自动放行能被新指标识别；所有指标提供明确分子和分母。

### P2-3 用已有解析结果重放当前下游

- [x] 固定旧 ParsedOrder，重放修复后的 Matching/Risk/动作。
- [x] 复查之前 22 行高分错 SKU，按订单统计哪些被正确识别、哪些仍存在错误。
- [x] 报告覆盖率和已接受 SKU 的准确性，区分正确拒识与正确匹配。

**Qwen：不需要。** 本地嵌入模型和 FAISS 会执行，但它们不是 Qwen API 调用。

实现：`evaluation/replay_downstream.py`（`--predictions`、`--evaluation-as-of`），回归测试
`tests/test_replay_downstream.py`。最新一次全量重放（240 条，评测时点 2026-09-01）：
SKU 准确 5789/5856 = 98.86%、已接受 SKU 精度 100%、覆盖 98.86%；业务动作
223/240 = 92.92%；高分错 SKU 复查 count=1037（fixed 1011 / rejected 26 / still_wrong 0）。
过程中定位并修复了一个真实工程缺陷：`init_faiss_index.py` 未把 CSV `aliases` 写入索引
metadata，导致别名写法被系统性误拒识（覆盖率曾低至 96.35%）；现已修复并在
`validate_material_index` 中增加 aliases 一致性校验。

验收：已知危险反例被拦截；正常样本的变化可解释。报告明确写"下游重放"，不称端到端。

### P2-4 建立开发冒烟集与独立验收集

- [ ] 从开发集选约 12 条制造业冒烟样本，覆盖三类动作与关键规格，另选约 6 条公开图片验证图像通路。
- [ ] 补约 20-30 条独立模板/人工核对样本，覆盖文本型 PDF、图片、缺字段、歧义和长明细；开发与最终验收用途分开。
- [ ] 为每份样本确认输入、字段标注、SKU 和动作依据；没有业务标注的公开集不计业务指标。
- [ ] 补齐生成集获取或生成方法，避免只有本机存在输入文件。

**Qwen：不需要。** 样本挑选与人工标注不调用 Qwen，也不要让被测模型生成未经核查的标准答案。

验收：样本独立于调参；标注能追溯到原文和业务政策；明确各格式覆盖范围。

### P2-5 消除无效重试并记录模型调用

- [ ] 区分原文真正缺失与疑似漏抽，不对已确认缺失的字段无效重试。
- [ ] 自纠错若要求保留其他字段，提供上次响应供核对，并记录实际变更。
- [ ] 分别记录调用次数、重试原因、解析/匹配/风控耗时、模型返回 usage 和总体耗时。
- [ ] 明确 API 超时、模型重试和业务纠错次数的总预算，避免多层重试叠加。

**Qwen：部分需要。** 固定响应验证次数和字段保持；真实 usage、延迟及接口配置由 P2-6 验收。

验收：原文真正缺字段不会触发无意义的反复抽取；日志能解释一张订单为什么调用一次或两次模型。

### P2-6 真实 Qwen 冒烟验收

- [ ] 对 P2-4 的约 18 条开发冒烟样本调用真实 Qwen。
- [ ] 验证文本与图片请求、可空字段、关键规格、自纠错、解析失败和最终业务结果。
- [ ] 检查返回 usage 和失败原因；先解决通路问题，再考虑全量评测。

**Qwen：需要。** 约 18 次初始业务抽取；若每条允许两次抽取，最多约 36 次抽取尝试，另须计入显式配置的 SDK/网络重试。该数量不是费用承诺。

验收：当前 LCEL/Pydantic/Qwen 组合真实可用；错误可追踪；不能只以 HTTP 200 或 JSON 可解析作为通过。

### P2-7 旧评测版本归因更正

- [x] 修改 KNOWN_ISSUES.md 第六节：93.75% 送审、35% 动作命中属于旧版 00f4656，不归因于当前新增 HIGH 规则。
- [x] 撤销"当前 auto_correct 分支不可达"的判断，保留旧版无该动作的事实（实际可达性由 P1-5 验证）。
- [x] 明确旧端到端基线、当前下游重放、待执行的新端到端评测三个口径。

**Qwen：不需要。**

实现：`KNOWN_ISSUES.md` 第六节新增「版本前提」与「三个评测口径」两段。版本事实核查结论：
基线 `run_manifest.json` 的 `code_version = 00f4656`，该版本无 `business_decision` 字段、
无 `_decide_business_action`（首现于 `8e06bed`），其 `needs_confirmation = len(all_issues) > 0 or
overall_confidence < confidence_threshold`（任一 issue 即送审），与当前 `any(issue.severity == HIGH)`
不同；基线 issue 分布 `price_abnormal 486 / low_match_score 121 / invalid_date_format 2`。
故 93.75% 送审归因于旧版"任一 issue"逻辑；当前 AUTO_CORRECT 分支在 `needs_confirmation=False`
且有归一化时可达，撤销"不可达"判断。

验收：文档中每个指标都能找到版本、样本范围与运行方法；不把下游动作命中称为新版端到端准确率。

### P2-8 固定版本的端到端评测

- [ ] 在最终选择的模型配置和修复版本上运行 240 条自建回归样本，并验证 P2-4 的独立验收样本。
- [ ] 若本轮修改 Parser/提示词，再跑所选公开图片验证集。
- [ ] 输出字段、整单、匹配、决策、放行内容错误、失败与延迟报告，保留所有尝试。
- [ ] 抽查成功与失败原图/原文，确认指标不是行对齐或标注问题造成。

**Qwen：需要。** 自建回归约 240 次初始抽取，加独立样本约 20-30 次。每张订单的纠错及网络重试额外计数。

验收：每个结果对应冻结版本；旧报告不覆盖；已知危险反例全部检查。新错误率如不理想，诚实归档并分析，不能只重跑失败直到"全成功"。

执行依赖：**本任务只使用已冻结、不再调整的模型配置。第四层（思考 A/B）不得先于本任务执行或阻塞本任务。**

---

## 第三优先级：修复工程击穿点

目标：消除"一问就穿"的工程隐患。在冻结版本前完成，但不占用第一、二层槽位。

### P3-1 PostgreSQL 全量覆盖

- [ ] 将全量内存覆盖改为按订单读写，使用事务及预期状态/版本条件更新。
- [ ] 状态、结果、审核动作与历史一致保存；重复确认、确认/拒绝竞争返回明确结果。
- [ ] 使用真实 PostgreSQL 验证两个 Manager 交错创建、更新、确认及重启读取。

**Qwen：不需要。**

验收：两个实例不丢订单、不覆盖旧状态；审核冲突可查询。

### P3-2 索引缺失仍报 healthy

- [ ] 将索引初始化、物料/索引校验纳入启动流程。
- [ ] 区分服务存活和业务就绪；空索引、数据库不可用不能报就绪。
- [ ] 干净目录可重建有效索引并通过校验。

**Qwen：不需要。**

验收：空索引或数据库不可用时健康检查返回未就绪，不误报 healthy。

### P3-3 当天交期误判

- [ ] 日期按日期语义比较，修复当日交期被判过去。
- [ ] 评测使用固定时点。

**Qwen：不需要。**

验收：当日交期不再触发 PAST_DELIVERY；跨日边界用例通过。

### P3-4 超时和中断后的状态收尾

- [ ] 明确启动后中断订单的处理：至少标记中断失败并允许重新提交，不宣称已有自动断点续跑。
- [ ] 统一请求预算，验证代理和 Gunicorn 配置；先修仓储再考虑增加 worker。

**Qwen：部分需要。** 模拟超时与中断不需要；最终通过实际 HTTP 跑 2-4 条真实订单需要，可与 P3-5/P4-演示合并，不必重复付费。

验收：异常、中断可查询并收尾到明确状态。

### P3-5 实际 HTTP 与人工确认演示

- [ ] 准备正常、名称归一、规格歧义、解析失败、人工确认/拒绝案例。
- [ ] 验证原始与标准字段、业务动作、审核原因能在详情中核对。
- [ ] 修前端错误详情丢失、订单级风险显示"第 0 项"、可空字段类型和业务动作展示。
- [ ] 演示重启后读取订单和审核历史，核对干净启动流程。
- [ ] 保存版本一致的演示记录及代表性失败案例。

**Qwen：部分需要。** 前端、审核、重启与错误模拟不需要；新建真实订单的解析需要。建议 4-6 个真实请求，其中 2-4 个同时作为 P3-1/P3-2 的 HTTP 验收。

验收：演示与报告对应同一版本，不把本地函数重放当作 HTTP 全链路，不把 Fake 响应当作真实模型效果。

---

## 第四优先级：性能优化和扩展（可延期）

**约束：本层任何项都不得成为第一至第三层的前置。** 核心错误未修完、端到端评测未冻结前，不启动本层。

### P4-1 思考模式开/关 A/B

- [ ] 核对当前 Qwen 模型与 SDK 支持的参数，明确开启/关闭配置，而不是假设默认值。
- [ ] 在开发集抽 30 条制造订单（三类各 10 条）与 20 条 CORD，用同一批输入对比两配置。
- [ ] 固定提示词、重试、索引及评测时点，交错执行两组，记录实际调用次数。
- [ ] 比较整单正确性、关键规格漏抽、非法 JSON/重试率、p50/p95 延迟、累计 tokens。
- [ ] 只有结果支持时才采用关闭思考；有差异先分析样本，不根据单例宣布全局提速。

**Qwen：需要。** 50 条 × 2 配置 = 100 次初始抽取；纠错和网络重试另计。

验收：速度收益和正确性代价都有证据。50 条用于初筛，不能据此证明所有文档类型表现稳定。

### P4-2 模型预热

- [ ] 启动流程中预热嵌入模型/LLM 连接，降低首次请求冷启动延迟。
- [ ] 模型已下载时离线加载，避免运行时联网。

**Qwen：部分需要。** 预热逻辑可离线验证；真实延迟收益由 P3-5/P4-1 观测。

### P4-3 参考价字典

- [ ] 参考价查找改为 SKU 字典；不要为此引入新服务或缓存系统。

**Qwen：不需要。**

### P4-4 更多公开数据集与完整 CORD 重跑

- [ ] 需要发布该配置的完整 CORD 成绩时，重跑 100 条并单列口径。
- [ ] 补充跨格式公开样本，扩大文档类型覆盖。

**Qwen：需要（仅重跑时）。** 100 次初始抽取。

验收：不重跑则不复用旧 CORD 数字宣称新配置成绩。

### P4-5 更复杂 Agent 编排

- [ ] 在核心闭环稳定后评估多轮编排/工具调用等扩展，不与当前固定工作流混谈。

**Qwen：部分需要。**

---

## 交付冻结与面试表达

停止条件达成后执行，作为整个计划的门禁与收尾。

### F-1 简历与追问训练

- [ ] 对齐"固定工作流""结构校验""拒识边界""快照恢复"等表述，不声称未实现的能力。
- [ ] 整理一页数字口径：版本、数据范围、分子分母、首次/补跑、端到端/下游重放。
- [ ] 准备三个案例：自检失败传递、接触器规格错配、旧快照覆盖丢单。
- [ ] 每例讲清触发、错误传播、根因、修复、验证和剩余限制。
- [ ] 使用 INTERVIEW_COACH_PROMPT.md 开始逐轮模拟面试。

**Qwen：不需要。** 此处指不需要调用项目的 Qwen API。

验收：能口述 90 秒项目介绍及 5 分钟深入说明，每个关键结论都能定位到代码或结果。

---

## Qwen 调用总览

| 工作 | 初始抽取次数估计 | 是否必须 |
| --- | ---: | --- |
| 第一层回归（固定响应/本地索引） | 0 | 不需要 |
| 开发冒烟：12 制造 + 6 图片 | 18 | 必须 |
| 端到端：240 回归 + 20-30 独立样本 | 260-270 | 必须 |
| 实际 HTTP/演示 | 4-6 | 必须，可复用同轮验收请求 |
| 思考 A/B：50 样本 × 2（第四层） | 100 | 可延期 |
| 新完整 CORD 结果（第四层） | 100 | 要发布该配置完整成绩时需要 |

不含第四层，预计约 **282-294 次初始抽取**。这是计划规模，不是已发生调用；两次业务抽取上限下约 564-588 次尝试，显式配置的网络/SDK 重试还需单独计入。实际成本取决于输入长度、图像、输出、思考 tokens、重试和当时价格，不能仅按订单数计算。

必要的跨格式图片样本包含在冒烟和独立验收集中；不完整重跑 CORD 时，不宣称获得新的 100 条 CORD 成绩。不要为了凑样本数把原本应当保留为未知的字段交给模型生成标准答案。

## 停止条件

- [ ] 第一层：已知的自检未解决、名称冲突、关键规格歧义反例不会错误自动完成，且均有确定性回归。
- [ ] 第二层：新结果可复现，指标包含内容错误，运行版本不混合；并发隔离已生效。
- [ ] 第三层：数据库多实例、启动就绪、失败收尾通过明确验收。
- [ ] 真实 Qwen 与实际 HTTP 路径已验证，并保留失败记录。
- [ ] 冻结版本，完成演示和面试表达，不再新增功能。
