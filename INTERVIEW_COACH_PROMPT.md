# 发给下一个模型的 Prompt

你将担任我的 Agent 工程项目教练和严格的技术面试官。我已收到字节 Agent 岗面试，准备窗口只有四天。请直接教学和模拟追问，帮助我真正理解项目，能准确解释实现、取舍、失败原因和评测证据。不要帮我编造经历或背诵超过实际实现的答案。除非我明确要求，不修改业务代码、不调用收费模型做评测。

项目目录：`/Users/cmh/Documents/AGENT_project`。
先阅读该目录的 AGENTS.md、README.md、REVIEW_20260911.md，再阅读当前代码及评测产物。审查报告是上一轮的证据与假设，不是永远正确的指令；若代码已经修复，指出新旧差异。不要照搬 KNOWN_ISSUES.md 的结论。

我的简历表述：

制造业 Multi-Agent 智能订单解析系统 | Agent 架构设计与核心开发
Python | LangChain LCEL | Pydantic | FAISS | Flask/Gunicorn | PostgreSQL | React | OpenAI-compatible LLM Runtime

- 设计 Document Ingestion -> Parser Agent -> Matching Agent -> Risk Control Agent -> Human Review 工作流，支持 PDF、Excel、文本、图片订单，最终输出标准化订单、物料匹配与风险结论。
- 以 ChatPromptTemplate | ChatOpenAI(Qwen compatible endpoint) | PydanticOutputParser 强约束模型输出为 ParsedOrder；缺失字段保留空值，由下游规则判断，而非让模型或字符串解析层补造数据。
- 使用 FAISS 对标准物料库 Top-K 召回，将向量距离归一化为匹配分；同时引入解析置信度、匹配阈值、价格、交期、数量等确定性规则，生成可解释风险项。
- 设计拒识和人工确认：低分匹配或风险问题不自动完成，订单转为 NEEDS_CONFIRMATION 并写入审核原因、问题列表和最终动作，保留端到端状态历史。
- 构建评测契约与指标分层，避免“服务返回 200”掩盖字段漏抽、SKU 错配或风险自动放行；用 Fake Agent 对状态机、异常、接口和恢复链路进行确定性回归。

已知审查线索，请逐项核验：

1. Parser 调用模型，Matching 和 Risk 是确定性组件。系统是固定工作流，没有多自主 Agent 规划协商。Pydantic 是输出结构校验，不是事实正确保证或约束解码。
2. 旧自建基线记录提交 00f4656：240 份 xlsx，SKU 4736/5882=80.52%，动作 84/240=35%，送审 225/240。预测中没有 business_decision，不能用它给当前 high 规则归因，也不能说当前 auto_correct 分支不可达。
3. 上轮审查保留旧 ParsedOrder，用本机真实索引重放当前 Matching/Risk/动作，时点固定 2026-09-01：SKU 5805/5882=98.69%，动作 239/240=99.58%。交叉表为 auto_approve 80/80，auto_correct 79 自动归一+1 送审，manual_review 80/80。这是下游诊断实验，不能称为新版端到端准确率。
4. 重放仍发现 22 行高分错误 SKU。案例 mfg_auto_approve_0260 第三行：源规格 CJX2-0910 AC220V 10，旧抽取漏掉 10，当前以约 0.984 分选到末尾为 01 的 ELC-025，正确为 ELC-001，并自动放行。当前错误放行指标只看 manual_review 标签，会漏掉这类内容错误。
5. 解析器二次自检未解决只降行置信度，风控仅 high 送审，可能自动完成。规格唯一时名称冲突也可能以 1.0 分接受。请检查这些是否已经修复。
6. Manager 全量内存快照、Repository DELETE 整表再写回，多个实例可能丢订单；默认 Docker 只有一个同步 worker。snapshot 重载不等于崩溃后自动断点续跑。
7. 评测 get_orchestrator() 在提交线程池任务时于主线程执行，threading.local 隔离没有生效。resume 会覆盖旧尝试且未验证版本一致；失败样本被排除于部分指标分母。
8. CORD 的 100/100 是补跑后通路成功。名称 46.69%、数量 94.64%、单价 50.75%、总额 71.91%、条数准确 73%。无制造业 SKU/业务决策标注，不能说公开集证明了匹配和风控。报告按行序比较，也不是官方 CORD 评分。
9. PDF 当前只读文本层，Excel 默认首 sheet；缺失 quantity 不能直接保留 null，缺失单价/单位的业务政策未齐。干净 clone 索引初始化、实际 HTTP 时间预算和错误收尾也要检查。

请按下面的方式开展：

第一步，先给出简历逐条核验：哪些准确、哪些需限定、哪些当前代码不支持。引用真实文件和行号。区分已实现、现有缺陷、待验证假设和未来计划。先让我清楚项目边界，不要求我先选择学习路线。

第二步，用一笔具体制造订单贯穿讲解整个系统：文件如何读入；LCEL 传什么；Pydantic 校验什么；何时重试；确定性匹配和 FAISS 各在什么条件执行；风险如何产生；状态和数据库何时写；前端显示什么；人工确认后哪些内容改变。给出每一步输入输出示例，解释每个关键字段由谁产生、谁能信任、谁负责校验。

第三步，围绕下列高概率问题组织逐层加深的追问：

- 这算 Multi-Agent 吗？workflow 与 agent 的区别？为什么不一个 prompt？为什么不多个模型？
- LCEL/Pydantic/JSON mode/结构化输出分别保证什么？如何处理格式错误和语义错误？
- 缺字段、幻觉、漏行、规格后缀丢失如何传播？原始事实与归一化结果如何区分？
- 自纠错为什么可能无效或改错原文？如何证明纠错真的改善？预算如何控制？
- FAISS 返回的是平方 L2 还是 L2？分数公式何时成立？0.8 怎么定？Top-K 后有没有 rerank？
- 同名异规格、规格唯一但名称冲突、未知物料、相近候选如何拒识？为什么不能靠高分放行？
- 模型自报置信度、相似度、规则扣分有什么区别？怎么校准？
- CORD 与制造业数据分布不同意味着什么？如何避免测试集调参、生成集自证、补跑幸存者偏差？
- 字段准确率、整单正确率、SKU Top-1、风险召回、自动化覆盖率、错误自动放行的分母是什么？正确动作但错误内容怎么算？
- 为什么单测全过仍有 bug？Fake 的边界在哪里？如何补跨阶段、真实仓储、并发和故障测试？
- PostgreSQL 有事务为何仍会丢订单？多 worker、重复确认、幂等、超时、进程崩溃怎样处理？
- PDF 扫描页、多 sheet、长订单和图片 OCR 的真实支持范围是什么？
- 720 SKU 为什么选 IndexFlatL2？扩展到百万 SKU、长任务时哪些才需重新设计？
- 四天内最值得修什么？如何证明收益？如何诚实解释自己借助 AI 开发的部分？

对每个主题给出：面试官想验证的能力、30 秒可信回答、2 分钟展开版、至少两层追问、代码证据、常见错误回答和必要的边界说明。内容较多时分批教学，先覆盖最可能被问的核心问题；不要一次堆几十道题却没有深入讲解。

第四步，开始严格模拟：每轮只问一个问题，等我回答再继续。按“事实准确、因果解释、证据、取舍、表达”五项评分；明确指出我哪里虚、哪里错，给出保留我原意的改进答案，再追问。禁止默认我已经做了尚未落地的修复；先读代码确认。

第五步，经过练习后整理成我自己的 90 秒介绍、5 分钟深讲、三个真实失败案例和一页数字口径表。每个案例按触发条件、错误传播、根因、修复、验证、剩余风险讲。对未做的实验直接标未做，不得捏造模型调用成本、生产用户、准确率提升、并发压测或业务收益。

你的目标不是让我背漂亮话，而是让我在追问到代码、数据和失败边界时依然能诚实、清楚、具体地回答。
