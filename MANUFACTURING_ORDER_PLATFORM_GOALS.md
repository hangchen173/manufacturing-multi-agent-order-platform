# 制造业订单平台提升计划
## 面向后端开发与 Agent 开发岗位

## 一、目标与原则

本次提升不以增加 Agent 数量为目标，而是完成三个可证明的能力：

1. **可靠执行**：订单处理异步化，重复提交、进程重启和任务重试不会破坏状态。
2. **可靠决策**：能识别抽取错误、SKU 歧义和业务风险，不靠模型自报置信度决定自动通过。
3. **可靠评测**：明确区分请求成功、抽取正确、匹配正确和业务决策正确。

以下所有性能和质量目标都是待验证目标，不是已有成绩。

---

## 二、阶段一：修正评测口径，建立可信基线
**优先级：P0｜预计 3–4 天｜后端与 Agent 岗共同基础**

### 1. 修复标注与评测代码的不一致

当前生成器把人工确认标签放在 `business_decision` 中，而评测代码读取的是 `confirmation`。先统一数据契约，避免指标为空或统计失真。

具体工作：

- 定义订单、物料行、风险、最终动作的标注结构。
- 明确 `auto_approve`、`auto_correct`、`manual_review` 的判定条件。
- 评测启动前校验标签，缺字段直接失败，不能静默跳过。
- 漏行、多行和未知 SKU 都进入指标分母，不能只比较两边较短部分。
- 字段抽取、SKU 匹配、人工分流分别评分。

### 2. 校验黄金标签是否成立

重点检查：

- “冲突规格”是否真的有两项互相矛盾的证据，而非仅删掉部分规格。
- “非包装整数数量”是否在订单或主数据中有明确包装约束。
- 缺失规格的订单是否应该保留多个候选，而不是强行标成唯一 SKU。
- 合计异常是否有对应的订单级风险标注。
- 时间相关风险是否使用固定评测时点，避免数据集随日期变化而失效。

没有业务依据的风险样本，重新生成或删除，不为保留数量而保留错误标签。

### 3. 检查 SKU 数据与索引一致性

- 对比 CSV 和索引元数据的 SKU、规格、参考价。
- 记录主数据版本、索引版本和内容摘要。
- 初始化时检查版本一致性，不一致时明确报错。
- 参考价按 SKU 精确查询，不再把 SKU 转成向量后二次检索参考价。

### 4. 跑基线并归档

先用 dev 集中覆盖不同格式、决策类型和明细规模的一小批样本验证评测链路，再扩大范围。

输出：

- 字段准确率、明细召回率。
- SKU Top-1 准确率、未知物料误匹配率。
- 人工审核召回率、错误自动放行率。
- 自动处理覆盖率。
- 单订单耗时、模型调用次数、可获取的 Token 消耗。
- 每单原始预测、标签和错误原因。

### 验收标准

- 手工核对至少 10 份预测，确认指标计算与人工判断一致。
- 有一份真实基线报告，而不是只有评测脚本。
- 同一份报告可追溯到数据、代码、Prompt 和模型配置版本。
- 不用 test 集调阈值或 Prompt。

**面试成果：**能回答“系统到底在哪些情况下有效，哪些情况下不可靠”。

---

## 三、阶段二：完成可靠的异步订单处理
**优先级：P0｜预计 5–7 天｜后端岗核心亮点**

### 1. 将上传与处理解耦

目标链路：

```text
上传文件
→ 持久化订单和待处理任务
→ 返回 order_id
→ Worker 执行解析、匹配、风控
→ 前端查询任务状态
→ 展示结果或人工确认
```

- 上传接口返回 `202 Accepted`，不等待模型完成。
- 初版前端轮询即可，不必同时建设 WebSocket 和 SSE。
- 不用继续增大 Nginx 超时时间掩盖同步阻塞。

### 2. 实现持久化任务机制

先利用现有 PostgreSQL 评估实现一个边界清晰的任务队列，不同时引入多个消息中间件。

需要明确：

- 任务领取和并发抢占规则。
- 任务租约、执行超时和进程退出后的恢复。
- 重试次数与下一次执行时间。
- 可重试错误和不可重试错误。
- 超出重试次数后的终态与人工重跑入口。

采用“至少一次执行 + 幂等状态更新”，不要声称实现了无法证明的端到端恰好一次执行。

### 3. 保证状态一致性

- 订单创建和任务创建处于同一事务。
- 数据库作为订单状态的权威来源。
- 使用状态前置条件或版本号控制更新。
- 同一订单不能被重复完成。
- 两个人同时确认订单，只允许一次有效状态转换。
- Worker 重启后不能让任务永久卡在 `processing`。

### 4. 处理重复提交

- 明确幂等键的作用域和有效期。
- 同一幂等键、相同输入返回同一任务。
- 同一幂等键、不同输入返回冲突。
- 文件摘要用于关联和诊断，不简单禁止同一个文件再次做评测。

### 验收标准

使用可控模型替身进行故障注入，并补充真实模型冒烟测试：

- 多份订单提交时，上传接口无需等待推理结束。
- 同一幂等键并发提交，只创建一个订单任务。
- 执行中强制停止 Worker，重启后任务可恢复。
- 模型超时触发有限重试；非法输入不重复重试。
- 并发人工确认不产生重复终态。
- 无需重建镜像就能完成普通服务启停。

**面试成果：**可演示任务恢复、并发抢占、幂等和事务一致性。

---

## 四、阶段三：让复杂 Excel/PDF 成为真实技术能力
**优先级：P1｜预计 5–7 天｜Agent 岗核心亮点**

### 1. 先增加结构差异，而不是只换配色

选择少量真正不同的订单模板：

- 首 Sheet 是封面，后续 Sheet 才是订单明细。
- 同一订单分布在多个 Sheet。
- 多级合并表头，明细区域中有空行、说明和小计。
- 列顺序不同，表头使用不同业务名称。
- 一项物料跨行描述，规格在下一行补充。
- 公式金额、缓存值缺失或缓存值过期。
- PDF 跨页明细、重复表头、扫描页与文本页混合。

每种模板保留明确黄金标注和版式编号。

### 2. 建立统一文档中间表示

文档读取阶段保留：

- 工作表名或 PDF 页码。
- 单元格坐标或文本区域位置。
- 原始文本。
- 合并区域、表格区域等必要结构信息。

让抽取结果能追溯到来源，而不是只输出一个 JSON。

### 3. 实现内容路由和校验

- XLSX 按工作表和区域读取，不再只读第一张表。
- 隐藏工作表采用明确规则，并记录排除情况。
- 合并单元格不能全表盲目向下填充。
- 小计、总计与真实物料行分开处理。
- PDF 先判断文本可用性，对缺少可用文本的页面走图像解析。
- 公式没有可用结果时显式标记，不编造金额。

### 4. 加入有边界的二次抽取

触发条件来自验证结果，例如：

- 明细数量不一致。
- 必填规格缺失。
- 数量、单价、金额关系不成立。
- 结果无法定位到来源区域。

只重试问题区域，设置最大次数；仍不确定时转人工，不做无限“反思循环”。

### 验收标准

- 每种结构至少有一个固定回归样本。
- 文档加载器、结构化抽取分别有测试。
- 每个输出物料行能定位到源 Sheet/行或 PDF 页。
- 报告按模板分别统计漏行、错列和金额错误。
- 通过消融实验验证结构信息和二次校验是否真的改善结果。

**面试成果：**能解释“为什么普通 read_excel 不够，以及每项增强解决了什么错误”。

---

## 五、阶段四：升级 SKU 匹配和业务风控
**优先级：P1｜预计 4–6 天｜Agent 岗最有区分度的部分**

### 1. 先建立可解释基线

比较：

- 名称及规格精确匹配。
- 当前向量 Top-1。
- 规范化 + 候选召回 + 约束校验。

不要默认增加重排模型就一定更好。

### 2. 将关键规格作为约束

围绕当前物料类别实现必要的归一化与校验：

- 单位、全半角、分隔符。
- 型号和尺寸数字。
- 材质、接口、屏蔽类型等关键属性。
- 同名但关键规格不同的候选拒绝自动匹配。

不必一次支持所有工业规格；先选择错误最多的几类做透。

### 3. 增加拒识和歧义判断

- 向量相似分数不命名为“正确概率”。
- 结合候选分差、规格冲突和信息完整度决定是否自动接受。
- 未知物料不能硬选最相近的 SKU。
- 返回候选及拒绝自动决策的原因。

阈值只在 dev 集调优，说明误放和人工量之间的取舍。

### 4. 补全确定性业务检查

- 数量 × 单价与行金额。
- 行金额汇总与订单总额。
- 参考价按准确 SKU 获取。
- 单位一致性。
- 有明确依据的包装数量约束。
- 可固定时点的交期检查。

涉及金额采用一致的精度和舍入规则。

### 验收标准

- 有同名不同规格的困难负样本集。
- 报告 SKU 准确率、人工审核召回率、错误自动放行率和自动处理覆盖率。
- 不以“全部转人工”换取好看的风险召回率。
- 对比改造前后结果，并保留失败案例。
- 如果测试中没有误放，表述为“该测试集上未观察到误放”，不宣称线上零风险。

**面试成果：**能回答“什么时候应该相信检索结果，什么时候必须拒绝”。

---

## 六、阶段五：补齐工程验证与安全边界
**优先级：P1｜预计 3–4 天｜后端岗加分项**

### 1. 分层测试

- 单元测试：状态、金额、规则和阈值边界。
- 集成测试：真实 PostgreSQL、任务抢占、并发确认。
- 文档回归：固定 Excel/PDF 样本。
- 真实模型评测：单独运行并保存结果，不混入快速单元测试。

### 2. 基础安全

- 身份认证，以及订单读取和确认的权限边界。
- 文件扩展名与实际格式校验。
- XLSX 解压大小、Sheet 数、行列规模等资源限制。
- 文件访问路径限制。
- 上传文件保留策略与订单记录关联，不能当普通缓存随意删除。
- 日志不记录密钥，避免无必要保存完整订单敏感内容。

### 3. 可观测性和性能

记录：

- `order_id`、任务尝试次数、阶段。
- 排队时间、解析时间、匹配时间、总耗时。
- 模型错误、重试、限流和终态原因。

分别进行：

- 使用模型替身的后端并发测试。
- 使用真实模型的有限规模延迟与成本测试。

不能把替身压测结果当成真实模型吞吐能力。

### 验收标准

- 有自动运行的测试入口和持续集成检查。
- 有一份性能报告，写清环境、并发、样本规模和限制。
- 错误能够按订单定位到阶段与原因。
- 无鉴权用户不能读取或确认其他用户的订单。

---

## 七、阶段六：形成可面试的成果包
**优先级：P0｜预计 2–3 天**

最终交付：

1. **一张架构图**：请求、任务、模型、索引和数据库之间的关系。
2. **一份基线与改进报告**：包含质量、延迟、成本和失败案例。
3. **三份设计决策记录**：
   - 为什么选当前异步任务方案。
   - 为什么采用约束校验而非直接相信向量 Top-1。
   - 为什么部分文档必须转人工。
4. **一组可靠性演示**：重复提交、Worker 重启、模型超时。
5. **一组文档演示**：复杂多 Sheet、跨页 PDF、未知 SKU。
6. **两套简历表述**：使用同一项目事实，分别突出后端和 Agent 能力。

### 后端岗重点讲

- 数据库事务和状态一致性。
- 任务领取、租约和恢复。
- 幂等与并发控制。
- 性能测试和故障排查。

### Agent 岗重点讲

- 文档结构保留与来源追踪。
- 结构化输出校验和有限重试。
- SKU 消歧、拒识和风险控制。
- 数据集设计、指标口径和消融实验。

---

## 八、明确暂不做的事情

以下内容不作为本轮提升目标：

- 为了“多 Agent”增加角色数量或自由对话。
- 同时引入多种队列、向量数据库和检索引擎。
- 没有误差分析就做微调。
- 没有单机瓶颈证据就拆微服务、上 Kubernetes。
- 用增加超时时间代替异步化。
- 只增加合成样本数量，不增加结构差异。
- 只展示成功案例，不展示错误和限制。

## 完成标准

项目升级完成，不是“新增功能全部写完”，而是：

> 有可信基线、有可靠任务执行、有可解释拒识、有真实格式回归，并能用测试和报告证明这些能力。